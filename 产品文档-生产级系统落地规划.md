# 智能审理系统 — 生产级系统落地规划文档

> **版本**: v1.0  
> **编制日期**: 2026-05-14  
> **编制**: 小墨 (Xiao Mo)  
> **状态**: 待评审

---

## 执行摘要

本文档是对"AI 驱动的纪检监察智能审理一体化平台"的**真实生产就绪度评估**和**可落地实施规划**。

### 现状一句话总结

> 当前系统处于 **"骨架搭建完成，核心功能空缺，前端为演示壳"** 的 **Alpha 阶段**（0-1 的 15%）。

### 代码规模

| 组件 | 行数 | 状态 |
|------|------|------|
| Java 后端 | ~6,000 行 | 骨架代码，大量 Service 实现缺失 |
| Vue 前端 | ~2,600 行 | 演示页面，无真实 API 对接 |
| SQL | ~586 行 | 基础表结构完整，缺少索引和优化 |

### 核心差距

| 维度 | 现状 | 生产要求 | 差距 |
|------|------|----------|------|
| **用户认证** | AuthController 有接口，IAuthService 未实现 | JWT 登录 + 权限拦截 + 密码加密 | 🔴 0% |
| **权限控制** | 实体表有，拦截器未启用 | RBAC + 五级定密 + 数据级权限 | 🔴 0% |
| **文档解析** | 解析框架有，依赖问题多 | Word/PDF 真实解析 → AI 分类 → 向量入库 | 🟡 40% |
| **多库管理** | Service 实现完整，但无前端对接 | 四库 CRUD + 搜索 + 版本管理 | 🟡 60% |
| **类案推送** | 代码不存在 | 向量相似度 + 多维度匹配 | 🔴 0% |
| **文书生成** | 代码不存在 | 模板引擎 + AI 辅助生成 | 🔴 0% |
| **处分执行** | 代码不存在 | 工作流引擎 + 状态机 | 🔴 0% |
| **前端页面** | 静态演示页面 | 真实数据对接 + 完整交互 | 🔴 10% |

---

## 一、系统现状深度分析

### 1.1 后端模块分析

#### common 模块（公共工具）
| 组件 | 状态 | 说明 |
|------|------|------|
| BaseEntity | ✅ 可用 | 基础实体，含 createTime/updateTime |
| R<T> 统一返回 | ✅ 可用 | 标准的成功/失败响应 |
| PageRequest/PageResult | ✅ 可用 | 分页请求和响应 |
| GlobalExceptionHandler | ✅ 可用 | 全局异常处理 |
| BusinessException | ✅ 可用 | 业务异常 |

**评价**: common 模块质量良好，可直接用于生产。

#### auth 模块（认证授权）
| 组件 | 状态 | 说明 |
|------|------|------|
| AuthController | ⚠️ 部分可用 | login/logout 接口定义完整，但 getUserInfo 返回 null |
| IAuthService | ❌ 不存在 | **整个 Service 接口未实现** |
| JwtUtil | ⚠️ 存在 | JWT 工具类存在，但未验证 |
| JwtInterceptor | ⚠️ 存在 | 拦截器存在，但未注册到 Spring |
| PasswordEncoderUtil | ⚠️ 存在 | BCrypt 加密，需验证 |
| LogAspect | ✅ 存在 | AOP 日志切面 |
| SecurityConfig | ❌ 空壳 | 未配置 Spring Security |

**评价**: auth 模块是**最大的短板**，整个认证流程未打通。有 Controller 和工具类，但 Service 层完全缺失。

#### document 模块（文档解析）
| 组件 | 状态 | 说明 |
|------|------|------|
| DocumentParseController | ✅ 可用 | 上传/查询/重试接口完整 |
| DocumentParseService | ✅ 可用 | 异步解析流程完整 |
| LlmClient | ✅ 可用 | DashScope API 调用完整 |
| WordParseUtil | ⚠️ 需修复 | POI API 兼容性问题已修复，需测试 |
| MinioUtil | ✅ 可用 | MinIO 上传下载 |
| AsyncConfig | ✅ 可用 | 异步线程池配置 |
| DocParseTask | ✅ 可用 | 解析任务实体 |

**评价**: document 模块是**质量最好的模块**，核心逻辑完整。但：
- 数据库表 `doc_parse_task` 需验证是否存在
- 向量入库部分标记为 TODO（Milvus 未对接）
- PDFBox 解析逻辑存在，未测试

#### repository 模块（多库管理）
| 组件 | 状态 | 说明 |
|------|------|------|
| DocumentController | ✅ 可用 | CRUD + 搜索 + 下载 + 预览 |
| DocumentService/Impl | ✅ 可用 | 实现完整，含 MinIO 集成 |
| FileDownloadResult | ✅ 可用 | 内部类，文件下载封装 |
| DirectoryController | ⚠️ 存在 | 目录管理接口，需验证 |
| DirectoryService/Impl | ⚠️ 存在 | 目录树逻辑，需验证 |
| searchDocuments | ⚠️ 需验证 | Mapper 中的复杂查询，需验证 SQL |

**评价**: repository 模块后端实现完整度较高，但：
- 前端未对接（Documents.vue 调用的是错误的 API 路径）
- searchDocuments 需要 XML Mapper 中定义 SQL

### 1.2 前端模块分析

| 页面 | 状态 | 说明 |
|------|------|------|
| Login.vue | ⚠️ 演示壳 | 页面美观，但 login() 是 mock 实现 |
| Layout.vue | ✅ 可用 | 简化版可用，原 Element Plus 版有渲染问题 |
| Dashboard.vue | ⚠️ 演示壳 | 静态数据，统计数字全部为 0 |
| Documents.vue | ❌ 未对接 | API 调用路径错误（/documents/list → /api/document/...） |
| DocumentUpload.vue | ❌ 未对接 | 上传组件完整，但后端 API 路径不匹配 |
| Categories.vue | ❌ 演示壳 | mock 数据，无真实 CRUD |
| repository/Index.vue | 存在 | 未在主路由中使用 |
| repository/Directory.vue | 存在 | 未在主路由中使用 |
| system/User.vue | 存在 | 未在主路由中使用 |
| system/Role.vue | 存在 | 未在主路由中使用 |
| system/Menu.vue | 存在 | 未在主路由中使用 |
| system/Log.vue | 存在 | 未在主路由中使用 |

**评价**: 前端有 12 个页面文件，但**只有 Layout.vue 和 Dashboard.vue 能正常显示**，且都是静态演示数据。真实页面存在但未被路由引用。

### 1.3 基础设施

| 组件 | 状态 | 说明 |
|------|------|------|
| Docker Compose | ✅ 可用 | MySQL/Redis/MinIO 容器正常 |
| MySQL | ✅ 运行中 | 但初始化 SQL 未执行，表为空 |
| Redis | ✅ 运行中 | 密码：redis123456 |
| MinIO | ✅ 运行中 | 控制台 http://localhost:9001 |
| Milvus | ❌ 未启动 | docker-compose 中未启用 |
| 后端 | ✅ 运行中 | 端口 8082，但数据库连接失败 |
| 前端 | ✅ 运行中 | 端口 3000，页面可访问 |

---

## 二、生产级系统定义

### 2.1 什么是"生产级"

一个生产级的智能审理系统必须满足：

| 维度 | 标准 |
|------|------|
| **功能完整性** | 架构方案中 8 大模块全部可运行，不是 mock |
| **数据真实性** | 能真实上传 Word 文档 → 解析 → AI 分类 → 展示结果 |
| **安全合规** | 用户认证通过、权限拦截生效、定密控制生效、操作日志完整 |
| **错误处理** | 文件上传失败有提示、解析失败可重试、网络异常有降级 |
| **性能要求** | 百级文档搜索 < 1s，文档上传 < 10s，并发支持 50+ 用户 |
| **可维护性** | 有 API 文档、部署文档、错误日志、监控告警 |

### 2.2 当前差距量化

| 模块 | 完成度 | 距离生产级还需要 |
|------|--------|------------------|
| auth 认证授权 | 5% | 实现 IAuthService、注册拦截器、密码加密验证、JWT 刷新 |
| document 文档解析 | 40% | 修复数据库表、测试 Word/PDF 真实解析、对接 Milvus 向量入库 |
| repository 多库管理 | 60% | 前端对接、验证 Mapper SQL、添加权限过滤 |
| 类案智能推送 | 0% | 从零开发：特征提取 + 向量相似度 + 多维度匹配 |
| 文书辅助生成 | 0% | 从零开发：模板管理 + AI 生成 + 审核流程 |
| 处分执行 | 0% | 从零开发：Flowable 工作流 + 状态机 + 提醒 |
| 前端完整对接 | 10% | 12 个页面全部对接真实 API，修复路由 |
| 测试 | 0% | 单元测试 + 集成测试 + E2E 测试 |
| 文档 | 20% | 需要 API 文档、部署文档、用户手册 |
| 安全审计 | 5% | SQL 注入防护、XSS 防护、CSRF 防护 |

**综合完成度: ~15%**

---

## 三、可落地的实施路线图

### 3.1 第一阶段：打通核心流程（2 周）

**目标**: 从"演示壳"变成"能用"

| 任务 | 详细说明 | 验收标准 | 预估工时 |
|------|----------|----------|----------|
| **1.1 初始化数据库** | 执行 init.sql，创建所有表 | SHOW TABLES 有 15+ 张表 | 0.5h |
| **1.2 修复后端启动** | 解决 MySQL 连接问题，确保 8082 端口正常 | curl /api/document/parse/tasks 返回 200 | 1h |
| **1.3 实现 IAuthService** | 实现 login/logout/getUserInfo/refreshToken | 前端能真实登录，返回 JWT token | 4h |
| **1.4 注册 JwtInterceptor** | 将 JWT 拦截器注册到 Spring MVC | 未登录请求返回 401 | 2h |
| **1.5 前端 Login 对接** | 修改 Login.vue 调用真实 /auth/login API | 输入 admin/admin123 能登录成功 | 2h |
| **1.6 前端路由修复** | 将真实页面接入主路由 | 12 个页面全部可访问 | 2h |
| **1.7 文档上传真实测试** | 上传一个 .docx 文件，验证解析流程 | 文件存入 MinIO，解析任务创建成功 | 3h |
| **1.8 Documents 页面对接** | 修复 API 路径，调用真实 /api/document/... | 列表显示真实文档数据 | 3h |

**第一阶段交付物**:
- ✅ 用户能真实登录
- ✅ 能上传 Word 文档并查看解析结果
- ✅ 多库管理页面显示真实数据
- ✅ 权限拦截生效

### 3.2 第二阶段：AI 能力集成（2 周）

**目标**: 从"能用"变成"智能"

| 任务 | 详细说明 | 验收标准 | 预估工时 |
|------|----------|----------|----------|
| **2.1 Milvus 向量数据库启动** | 修改 docker-compose，启动 Milvus | milvus:19530 端口可访问 | 2h |
| **2.2 向量入库实现** | 在 DocumentParseService 中对接 Milvus | 解析完成后，向量存入 Milvus | 6h |
| **2.3 AI 分类验证** | 上传法规文档，验证 LlmClient 分类效果 | 段落正确分类为总则/分则/附则/法律责任 | 2h |
| **2.4 类案推送 - 基础版** | 基于向量相似度实现类案检索 | 输入案件描述，返回 Top-5 相似案件 | 8h |
| **2.5 类案推送 - 多维度** | 加入金额、违纪类型、处理结果匹配 | 综合评分排序 | 6h |
| **2.6 文书模板管理** | 实现模板上传/下载/变量替换 | 能上传 .docx 模板，替换变量生成文书 | 6h |
| **2.7 AI 辅助生成** | 基于已有案件信息 + 模板生成文书初稿 | 一键生成审理报告初稿 | 8h |

**第二阶段交付物**:
- ✅ 文档解析后自动向量化
- ✅ 类案智能推送（输入案件 → 返回相似案件）
- ✅ 文书辅助生成（选择模板 → 生成初稿）

### 3.3 第三阶段：工作流与权限（2 周）

**目标**: 从"智能"变成"合规"

| 任务 | 详细说明 | 验收标准 | 预估工时 |
|------|----------|----------|----------|
| **3.1 权限系统完善** | 实现 RBAC 全链路：角色→菜单→按钮 | 不同角色看到不同菜单 | 8h |
| **3.2 五级定密控制** | 文档级/目录级/系统级权限控制 | 低密级用户无法查看高密级文档 | 6h |
| **3.3 Flowable 工作流** | 集成 Flowable，实现处分执行流程 | 处分决定 → 审批 → 执行 → 归档 | 12h |
| **3.4 操作审计日志** | 所有关键操作记录审计日志 | 可在日志页面查看谁在什么时候做了什么 | 4h |
| **3.5 数据级权限** | 用户只能看到自己部门的数据 | 部门 A 用户看不到部门 B 的案件 | 6h |
| **3.6 延期审批流程** | 处分延期申请 → 领导审批 | 完整的审批链 | 4h |

**第三阶段交付物**:
- ✅ 完整的权限体系（RBAC + 定密 + 数据隔离）
- ✅ 处分执行工作流（审批 → 执行 → 归档）
- ✅ 操作审计日志完整

### 3.4 第四阶段：生产就绪（2 周）

**目标**: 从"合规"变成"生产级"

| 任务 | 详细说明 | 验收标准 | 预估工时 |
|------|----------|----------|----------|
| **4.1 单元测试** | 核心 Service 层单元测试 | 覆盖率 > 60% | 16h |
| **4.2 集成测试** | API 接口集成测试 | 所有 API 通过测试 | 8h |
| **4.3 性能优化** | 数据库索引优化、查询优化 | 百级文档搜索 < 1s | 8h |
| **4.4 错误处理完善** | 全局异常处理、前端错误提示 | 所有异常情况有友好提示 | 6h |
| **4.5 部署文档** | Docker Compose 一键部署 | 新环境 30 分钟部署完成 | 4h |
| **4.6 API 文档** | Swagger/OpenAPI 文档 | 所有接口有文档和示例 | 4h |
| **4.7 用户手册** | 面向最终用户的操作指南 | 用户可以照着手册完成所有操作 | 8h |
| **4.8 安全加固** | SQL 注入、XSS、CSRF 防护 | 通过安全扫描 | 8h |

**第四阶段交付物**:
- ✅ 完整的测试套件
- ✅ 生产部署方案
- ✅ 完整的文档（API + 部署 + 用户手册）
- ✅ 安全合规

---

## 四、技术债务清单

### 4.1 紧急（必须修复）

| ID | 问题 | 影响 | 修复方案 |
|----|------|------|----------|
| TD-01 | IAuthService 未实现 | 用户无法登录 | 实现 login/logout/getUserInfo |
| TD-02 | JWT 拦截器未注册 | 无权限控制 | 注册到 WebMvcConfigurer |
| TD-03 | 数据库未初始化 | 所有查询报错 | 执行 init.sql |
| TD-04 | 前端 API 路径错误 | 页面无数据 | 统一修改为 /api/document/... |
| TD-05 | getUserInfo 返回 null | 登录后无法获取用户信息 | 实现真实查询逻辑 |

### 4.2 重要（尽快修复）

| ID | 问题 | 影响 | 修复方案 |
|----|------|------|----------|
| TD-06 | 密码未加密存储 | 安全风险 | 使用 BCrypt 加密 |
| TD-07 | 无 CSRF 防护 | 跨站请求伪造风险 | 添加 CSRF Token |
| TD-08 | 无输入验证 | SQL 注入风险 | 添加 @Validated + 参数校验 |
| TD-09 | 日志未记录到文件 | 无法排查问题 | 配置 logback 文件输出 |
| TD-10 | 前端路由未整合 | 部分页面无法访问 | 统一路由配置 |

### 4.3 一般（后续优化）

| ID | 问题 | 影响 | 修复方案 |
|----|------|------|----------|
| TD-11 | 无数据库索引优化 | 大数据量查询慢 | 添加复合索引 |
| TD-12 | 无缓存层 | 重复查询数据库 | 添加 Redis 缓存 |
| TD-13 | 无健康检查 | 无法监控服务状态 | 添加 actuator 端点 |
| TD-14 | 无备份策略 | 数据丢失风险 | 配置定时备份 |
| TD-15 | 前端无错误边界 | 错误导致白屏 | 添加 ErrorBoundary |

---

## 五、生产环境部署方案

### 5.1 架构调整

当前是单体应用，生产环境建议：

```
生产环境架构:
┌─────────────────────────────────────────────────┐
│                    Nginx (负载均衡)               │
├─────────────────────────────────────────────────┤
│  Auth 服务 (8081)  │  Document 服务 (8082)      │
│  Repository 服务   │  Workflow 服务 (8083)      │
├─────────────────────────────────────────────────┤
│  MySQL 8.0 (主从)  │  Redis 7.x (哨兵)          │
│  Milvus 2.4 (集群) │  MinIO (分布式)            │
└─────────────────────────────────────────────────┘
```

**第一阶段保持单体部署**，第二阶段再拆分微服务。

### 5.2 Docker Compose 生产配置

```yaml
# docker-compose.prod.yml
services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}  # 环境变量注入
    volumes:
      - /data/mysql:/var/lib/mysql  # 持久化到宿主机
    deploy:
      resources:
        limits:
          memory: 4G

  redis:
    image: redis:7-alpine
    command: redis-server --requirepass ${REDIS_PASSWORD} --appendonly yes
    volumes:
      - /data/redis:/data

  milvus:
    image: milvusdb/milvus:v2.4.0
    volumes:
      - /data/milvus:/var/lib/milvus
    deploy:
      resources:
        limits:
          memory: 8G

  app:
    build: .
    environment:
      SPRING_PROFILES_ACTIVE: prod
      DASHSCOPE_API_KEY: ${DASHSCOPE_API_KEY}
    deploy:
      replicas: 2  # 至少 2 个实例
      resources:
        limits:
          memory: 4G
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8082/actuator/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - /data/certs:/etc/nginx/certs  # SSL 证书
```

### 5.3 硬件配置

| 组件 | 开发环境 | 生产环境（最低） | 生产环境（推荐） |
|------|----------|------------------|------------------|
| CPU | 4 核 | 8 核 | 16 核 |
| 内存 | 8 GB | 16 GB | 32 GB |
| 存储 | 50 GB | 200 GB SSD | 500 GB SSD |
| GPU | 无 | 可选 | 1× NVIDIA A10（大模型推理） |
| 网络 | 局域网 | 内网隔离 | 内网隔离 + 专线 |

---

## 六、质量标准与验收

### 6.1 功能验收标准

| 功能 | 验收标准 | 测试方法 |
|------|----------|----------|
| 用户登录 | 输入正确用户名密码返回 JWT | POST /auth/login |
| 文档上传 | 上传 .docx 文件，10s 内返回解析任务 ID | POST /api/document/parse/upload |
| 文档解析 | 解析完成后，段落有 category 和 law_level | GET /api/document/parse/task/{id}/result |
| 类案推送 | 输入案件描述，返回 Top-5 相似案件 | POST /api/case/similar |
| 文书生成 | 选择模板 + 案件信息，生成 .docx 文件 | POST /api/document/generate |
| 权限控制 | 低密级用户访问高密级文档返回 403 | 用不同角色登录测试 |

### 6.2 性能验收标准

| 指标 | 要求 | 测试方法 |
|------|------|----------|
| API 响应时间 | P95 < 500ms | JMeter 压测 |
| 文档上传 | < 10s（5MB 文件） | 实际上传测试 |
| 文档解析 | < 30s（100 页 PDF） | 实际解析测试 |
| 搜索响应 | < 1s（1000 条记录） | 数据库查询测试 |
| 并发用户 | 支持 50 并发 | JMeter 并发测试 |
| 系统可用性 | 99.9%（每月停机 < 43 分钟） | 监控统计 |

### 6.3 安全验收标准

| 检查项 | 要求 | 工具 |
|--------|------|------|
| SQL 注入 | 所有查询使用参数化 | SQLMap |
| XSS 防护 | 输出编码 | Burp Suite |
| CSRF 防护 | Token 验证 | 手动测试 |
| 密码安全 | BCrypt 加密存储 | 数据库检查 |
| 日志脱敏 | 敏感信息不记录明文 | 日志审查 |
| 文件上传 | 限制类型和大小 | 上传恶意文件测试 |

---

## 七、风险与应对

### 7.1 技术风险

| 风险 | 概率 | 影响 | 应对方案 |
|------|------|------|----------|
| DashScope API 限流 | 中 | 高 | 添加重试 + 降级策略 |
| Milvus 稳定性 | 低 | 高 | 先使用 MySQL 全文搜索作为备选 |
| 大模型分类准确率 | 中 | 中 | 提供人工修正入口，收集反馈优化 prompt |
| PDF 解析质量 | 中 | 中 | 扫描件使用 OCR，文本 PDF 直接提取 |
| 前端性能 | 低 | 低 | 按需加载 + 分页 + 虚拟滚动 |

### 7.2 项目风险

| 风险 | 概率 | 影响 | 应对方案 |
|------|------|------|----------|
| 需求变更 | 高 | 中 | 采用敏捷迭代，每 2 周交付可用版本 |
| 人员不足 | 中 | 高 | 优先核心功能，辅助功能后续迭代 |
| 数据迁移 | 低 | 高 | 提前准备迁移脚本，测试环境验证 |
| 合规审查 | 中 | 高 | 提前与法务确认数据安全要求 |

---

## 八、下一步行动

### 8.1 立即执行（今天）

```bash
# 1. 初始化数据库
docker exec -i trial-mysql mysql -u root -proot123456 < ~/intelligent-trial-system/sql/init.sql

# 2. 验证表创建
docker exec trial-mysql mysql -u root -proot123456 -e "USE intelligent_trial; SHOW TABLES;"

# 3. 重启后端
pkill -f "DocumentApplication"
cd ~/intelligent-trial-system/document && mvn spring-boot:run -DskipTests &

# 4. 验证后端健康
curl -s http://localhost:8082/api/document/parse/tasks?page=1\&size=10
```

### 8.2 本周完成

- [ ] 实现 IAuthService（登录功能）
- [ ] 前端 Login 对接真实 API
- [ ] 修复所有前端 API 路径
- [ ] 实现真实的文档上传 → 解析 → 展示流程

### 8.3 两周完成（第一阶段目标）

- [ ] 用户能真实登录系统
- [ ] 能上传 Word 文档并查看解析结果
- [ ] 多库管理页面显示真实数据
- [ ] 权限拦截生效（未登录返回 401）
- [ ] 所有页面路由正常

---

## 附录 A：关键 API 清单

### Auth 模块
| 方法 | 路径 | 说明 | 状态 |
|------|------|------|------|
| POST | /api/auth/login | 用户登录 | ❌ Service 未实现 |
| POST | /api/auth/logout | 退出登录 | ❌ Service 未实现 |
| GET | /api/auth/info | 获取用户信息 | ❌ 返回 null |
| POST | /api/auth/refresh | 刷新 Token | ❌ Service 未实现 |

### Document 模块
| 方法 | 路径 | 说明 | 状态 |
|------|------|------|------|
| POST | /api/document/parse/upload | 上传并解析 | ✅ 可用 |
| GET | /api/document/parse/tasks | 查询任务列表 | ✅ 可用 |
| GET | /api/document/parse/task/{id} | 查询任务详情 | ✅ 可用 |
| GET | /api/document/parse/task/{id}/result | 查询解析结果 | ✅ 可用 |
| POST | /api/document/parse/task/{id}/retry | 重试失败任务 | ✅ 可用 |

### Repository 模块
| 方法 | 路径 | 说明 | 状态 |
|------|------|------|------|
| POST | /api/repository/documents | 创建文档 | ✅ 可用 |
| GET | /api/repository/documents | 搜索文档 | ✅ 可用 |
| GET | /api/repository/documents/{id} | 获取详情 | ✅ 可用 |
| PUT | /api/repository/documents/{id} | 更新文档 | ✅ 可用 |
| DELETE | /api/repository/documents/{id} | 删除文档 | ✅ 可用 |
| POST | /api/repository/documents/upload | 上传文件 | ✅ 可用 |
| GET | /api/repository/documents/{id}/preview | 预览文件 | ✅ 可用 |
| GET | /api/repository/documents/{id}/download | 下载文件 | ✅ 可用 |

---

## 附录 B：数据库表清单

| 表名 | 说明 | 状态 |
|------|------|------|
| sys_user | 用户表 | ✅ DDL 完整 |
| sys_role | 角色表 | ✅ DDL 完整 |
| sys_menu | 菜单表 | ✅ DDL 完整 |
| sys_dept | 部门表 | ✅ DDL 完整 |
| sys_user_role | 用户角色关联 | ✅ DDL 完整 |
| sys_role_menu | 角色菜单关联 | ✅ DDL 完整 |
| sys_classification_level | 定密级别 | ✅ DDL 完整 |
| sys_audit_log | 审计日志 | ✅ DDL 完整 |
| doc_document | 文档表 | ✅ DDL 完整 |
| doc_directory | 文档目录 | ✅ DDL 完整 |
| doc_parse_task | 解析任务 | ✅ DDL 完整 |
| doc_document_version | 文档版本 | ✅ DDL 完整 |
| case_info | 案件信息 | ✅ DDL 完整 |
| workflow_process | 工作流实例 | ✅ DDL 完整 |
| workflow_task | 工作流任务 | ✅ DDL 完整 |

---

> **文档版本历史**
> 
> | 版本 | 日期 | 变更说明 |
> |------|------|----------|
> | v1.0 | 2026-05-14 | 初始版本，真实评估当前状态 |
