<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.intelligent.trial.auth.mapper.SysAuditLogMapper">

    <resultMap id="LogVOMap" type="com.intelligent.trial.auth.vo.AuditLogVO">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="username" property="username"/>
        <result column="module" property="module"/>
        <result column="action" property="action"/>
        <result column="method" property="method"/>
        <result column="params" property="params"/>
        <result column="ip" property="ip"/>
        <result column="user_agent" property="userAgent"/>
        <result column="result" property="result"/>
        <result column="duration" property="duration"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="selectLogPage" resultMap="LogVOMap">
        SELECT
            l.id, l.user_id, l.username, l.module, l.action,
            l.method, l.params, l.ip, l.user_agent,
            l.result, l.duration, l.create_time
        FROM sys_audit_log l
        <where>
            <if test="module != null and module != ''">
                AND l.module LIKE CONCAT('%', #{module}, '%')
            </if>
            <if test="action != null and action != ''">
                AND l.action LIKE CONCAT('%', #{action}, '%')
            </if>
            <if test="userId != null">
                AND l.user_id = #{userId}
            </if>
            <if test="startTime != null and startTime != ''">
                AND l.create_time &gt;= #{startTime}
            </if>
            <if test="endTime != null and endTime != ''">
                AND l.create_time &lt;= #{endTime}
            </if>
        </where>
        ORDER BY l.create_time DESC
    </select>

</mapper>
