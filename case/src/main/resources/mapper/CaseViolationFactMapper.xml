<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.intelligent.trial.casemanage.management.mapper.CaseViolationFactMapper">

    <!-- 根据案件ID查询违纪事实列表（按排序字段排序） -->
    <select id="selectByCaseIdOrderBySort" resultType="com.intelligent.trial.casemanage.management.entity.CaseViolationFact">
        SELECT
            id, case_id, fact_title, fact_content, violation_type,
            occurred_date, amount, evidence, sort, create_time, update_time
        FROM case_violation_fact
        WHERE case_id = #{caseId}
        ORDER BY sort ASC, create_time DESC
    </select>

</mapper>
