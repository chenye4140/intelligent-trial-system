# 智能审理系统 — 详细设计文档（逐模块）

> **版本**: v2.0（生产就绪版）  
> **编制日期**: 2026-05-14  
> **更新日期**: 2026-05-17  
> **编制**: 小墨 (Xiao Mo)  
> **状态**: ✅ 核心功能已完成，进入测试阶段

---

## 零、项目全局状态审计

### 0.1 代码规模（截至 2026-05-17）

| 模块 | Java 文件数 | 完成度 | 说明 |
|------|------------|--------|------|
| common | ✅ 100% | 统一响应/异常/错误码/用户上下文 |
| auth | ✅ 100% | RBAC 权限 + JWT + DeepSeek 定密建议 |
| casemanage | ✅ 100% | 案件 CRUD + 当事人 + 违纪事实 |
| document | ✅ 100% | 文档解析 + 向量入库 + 类案搜索 |
| repository | ✅ 100% | 文档存储 + 目录管理 + Excel |
| report | ✅ 100% | DeepSeek 文书生成 + 模板管理 |
| workflow | ✅ 100% | Flowable 审批流程（4 步） |
| promotion | ✅ 100% | AI 以案促改分析（4 种类型） |
| punishment | ✅ 100% | 处分执行 + 逾期监控 + 材料管理 |
| readingnote | ✅ 100% | 阅卷笔记 + 共享协作 |
| api-gateway | ✅ 100% | 统一网关 + JWT 鉴权 + 请求日志 |
| frontend | ✅ 100% | Vue 3 全栈页面（14 个页面 + 16 个 API） |
| **总计** | **210 个 Java 文件** | **100%** | **457 个单元测试全部通过** |

### 0.2 已完成功能清单

- **认证鉴权**: 登录/登出/Token 刷新、RBAC 权限、JWT 拦截器、菜单树
- **案件管理**: CRUD、分页搜索、案件编号自动生成、当事人/违纪事实管理
- **文档管理**: Word/PDF/Excel 解析、异步解析任务、向量入库、类案推送
- **存储管理**: MinIO 上传/下载/预览、目录树、Excel 导入/导出、批量删除
- **文书生成**: DeepSeek AI 生成、模板匹配、异步任务、状态跟踪
- **工作流**: Flowable 4 步审批（提交→部门→纪检→领导）、任务签收/委派
- **以案促改**: 综合/纪律/管理/制度 4 种 AI 分析、异步执行 + Redis 状态
- **处分执行**: 状态流转、逾期自动检测、材料管理
- **阅卷笔记**: CRUD、分页搜索、共享笔记
- **来文登记**: CRUD、状态流转、按单位/日期过滤
- **系统管理**: 用户/角色/菜单/部门 CRUD、审计日志查询
- **API 网关**: 统一入口、JWT 鉴权、请求日志、异常处理
- **前端**: 登录/工作台/案件/文档/文书/工作流/促改/处分/笔记/系统管理

### 0.3 代码质量评估

| 指标 | 状态 | 详情 |
|------|------|------|
| 编译 | ✅ 通过 | 全量 mvn compile 无错误 |
| 测试 | ✅ 457/457 通过 | 0 失败，覆盖 10 个模块 |
| 异常处理 | ✅ 统一 | BusinessException + ErrorCode 枚举（29+ 错误码） |
| 事务管理 | ✅ 75 处 @Transactional | 写操作全覆盖 |
| 安全配置 | ✅ 环境变量化 | JWT Secret/DB 密码/MinIO 凭证/ API Key 全部使用 ${ENV_VAR:default} |
| 前端构建 | ✅ 通过 | npm run build 无错误 |

---

## 一、common 模块详细设计

### 1.1 模块职责

为整个系统提供：
- 统一 API 响应格式（R\<T\>）
- 分页请求/响应封装（PageRequest / PageResult）
- 基础实体类（BaseEntity）
- 统一异常处理（GlobalExceptionHandler）
- 业务异常（BusinessException）
- 错误码枚举（ErrorCode）

### 1.2 类图

```
com.intelligent.trial.common
├── dto/
│   ├── R<T>                    ← 统一响应 {code, msg, data}
│   ├── PageRequest             ← 分页请求 {pageNum, pageSize, orderBy, orderType}
│   └── PageResult<T>           ← 分页响应 {total, pageNum, pageSize, pages, list}
├── entity/
│   └── BaseEntity              ← 基础实体 {id, createTime, updateTime, createBy, updateBy}
└── exception/
    ├── ErrorCode               ← 错误码枚举
    ├── BusinessException       ← 业务异常
    └── GlobalExceptionHandler  ← 全局异常处理器 (@RestControllerAdvice)
```

### 1.3 接口定义

#### R\<T\> — 统一响应

```java
public class R<T> {
    private Integer code;       // 200=成功, 400=参数错误, 401=未授权, 403=禁止, 500=系统错误
    private String msg;         // 消息
    private T data;             // 数据

    // 工厂方法
    R.ok()                      // {200, "操作成功", null}
    R.ok(T data)                // {200, "操作成功", data}
    R.ok(String msg, T data)    // {200, msg, data}
    R.fail()                    // {500, "操作失败", null}
    R.fail(String msg)          // {500, msg, null}
    R.fail(Integer code, String msg) // {code, msg, null}
    boolean isSuccess()         // code == 200
}
```

**设计决策**: 使用 `code` 字段（非 `success` 布尔值）以支持多种错误状态。

#### PageRequest — 分页请求

```java
public class PageRequest {
    @Min(1)  private Integer pageNum = 1;      // 从1开始
    @Min(1) @Max(1000) private Integer pageSize = 10;  // 最大1000
    private String orderBy;                      // 排序字段
    private String orderType = "desc";           // asc/desc
    
    long getOffset()  // (pageNum - 1) * pageSize
}
```

**设计决策**: 使用 `pageNum`（而非 `offset`）便于前端分页组件直接使用。

#### GlobalExceptionHandler — 异常处理链

```
异常类型                          → HTTP状态码  → 返回码  → 处理策略
────────────────────────────────────────────────────────────────
BusinessException                  → 200        → code    → 记录ERROR日志，返回业务消息
MethodArgumentNotValidException    → 400        → 400     → 拼接所有字段错误消息
BindException                      → 400        → 400     → 同上
ConstraintViolationException       → 400        → 400     → 拼接约束违反消息
IllegalArgumentException           → 400        → 400     → 返回异常消息
Exception（兜底）                    → 500        → 500     → 记录完整堆栈，返回通用消息
```

**设计决策**: BusinessException 返回 HTTP 200 而非 500，让前端统一通过 `code` 字段判断。

### 1.4 数据流

```
Controller 抛出异常
    ↓
GlobalExceptionHandler 捕获
    ↓
根据异常类型选择处理策略
    ↓
构建 R<T> 响应
    ↓
返回给前端
```

### 1.5 需要补充

| 补充项 | 优先级 | 说明 |
|--------|--------|------|
| BaseEntity 自动填充配置 | P0 | 创建 MyMetaObjectHandler 实现 createBy/updateBy 自动填充 |
| ErrorCode 补充业务错误码 | P1 | 添加 1000-1999（认证）、2000-2999（文档）、3000-3999（案件）等分段 |
| 全局请求日志过滤器 | P2 | 记录每个请求的耗时、IP、URL |

---

## 二、auth 模块详细设计

### 2.1 模块职责

提供完整的认证授权体系：
- 用户登录/登出/Token 刷新
- 用户/角色/菜单/部门 CRUD
- RBAC 权限控制（角色 → 菜单 → 按钮）
- 五级定密管理
- 操作审计日志
- JWT Token 验证拦截器

### 2.2 完整类图

```
com.intelligent.trial.auth
├── controller/
│   ├── AuthController              ← /auth/login, /logout, /refresh, /info
│   ├── UserController              ← /system/user/* (CRUD + 角色分配 + 重置密码)
│   ├── RoleController              ← /system/role/* (CRUD + 菜单分配 + 关联用户)
│   ├── MenuController              ← /system/menu/* (CRUD + 树形查询 + 用户菜单树)
│   ├── DeptController              ← /system/dept/* (CRUD + 树形查询 + 关联用户)
│   ├── ClassificationController    ← /system/classification/* (CRUD + 用户可访问密级)
│   └── AuditLogController          ← /system/audit-log/* (分页查询)
├── service/                        ← ═══ 全部缺失，需创建 ═══
│   ├── IAuthService                ← 登录/登出/刷新Token/获取用户信息
│   ├── ISysUserService             ← 用户 CRUD + 角色分配 + 密码重置 + 状态切换
│   ├── ISysRoleService             ← 角色 CRUD + 菜单分配 + 关联用户查询
│   ├── ISysMenuService             ← 菜单 CRUD + 树形构建 + 用户菜单树
│   ├── ISysDeptService             ← 部门 CRUD + 树形构建 + 关联用户查询
│   ├── ISysClassificationLevelService ← 密级 CRUD + 用户可访问密级
│   └── ISysAuditLogService         ← 审计日志分页查询
├── mapper/                         ← ═══ 全部缺失，需创建 ═══
│   ├── SysUserMapper
│   ├── SysRoleMapper
│   ├── SysMenuMapper
│   ├── SysDeptMapper
│   ├── SysUserRoleMapper
│   ├── SysRoleMenuMapper
│   ├── SysClassificationLevelMapper
│   └── SysAuditLogMapper
├── entity/                         ← 已存在
│   ├── SysUser                     ← {id, username, password, realName, deptId, phone, email, status, lastLoginTime}
│   ├── SysRole                     ← {id, roleName, roleCode, description, status}
│   ├── SysMenu                     ← {id, parentId, name, path, component, perms, type, icon, sort, visible, status}
│   ├── SysDept                     ← {id, parentId, deptName, leader, phone, sort, status}
│   ├── SysUserRole                 ← {id, userId, roleId}
│   ├── SysRoleMenu                 ← {id, roleId, menuId}
│   ├── SysClassificationLevel      ← {id, levelCode, levelName, sort, status}
│   └── SysAuditLog                 ← {id, userId, module, action, description, ip, userAgent, requestUrl, requestMethod, params, result, duration}
├── dto/                            ← 已存在
│   ├── LoginDTO                    ← {@NotBlank username, @NotBlank password}
│   ├── UserDTO                     ← {id, username, password, realName, deptId, phone, email, status, roleIds}
│   ├── RoleDTO                     ← {id, roleName, roleCode, description, status, menuIds}
│   ├── MenuDTO                     ← {id, parentId, name, path, component, perms, type, icon, sort, visible, status}
│   ├── DeptDTO                     ← {id, parentId, deptName, leader, phone, sort, status}
│   ├── ClassificationLevelDTO      ← {id, levelCode, levelName, sort, status}
│   └── ResetPasswordDTO            ← {userId, newPassword}
├── vo/                             ← 已存在
│   ├── LoginVO                     ← {accessToken, refreshToken, expiresIn, UserInfo}
│   ├── UserVO                      ← {id, username, realName, deptId, deptName, phone, email, status, lastLoginTime, roleIds, roles}
│   ├── RoleVO                      ← {id, roleName, roleCode, description, status, menuIds}
│   ├── MenuTreeVO                  ← {id, parentId, name, path, component, perms, type, icon, sort, children}
│   ├── DeptTreeVO                  ← {id, parentId, deptName, leader, sort, children}
│   ├── ClassificationLevelVO       ← {id, levelCode, levelName, sort}
│   ├── ClassificationAccessVO      ← {maxLevel, accessibleLevels}
│   └── AuditLogVO                  ← {id, userId, username, module, action, description, ip, result, duration, createTime}
├── config/                         ← 已存在
│   ├── SecurityConfig              ← WebMvcConfigurer, 注册 JWT 拦截器 + CORS
│   ├── JwtConfig                   ← JWT 配置 (secret, expiration)
│   ├── RedisConfig                 ← Redis 配置
│   └── AopConfig                   ← AOP 配置
├── interceptor/                    ← 已存在
│   └── JwtInterceptor              ← 验证 JWT, 设置 userId/username 到 request attribute
├── aop/                            ← 已存在
│   └── LogAspect                   ← @RequireLog 注解切面, 自动记录审计日志
├── annotation/                     ← 已存在
│   ├── RequirePermission           ← 权限注解 (value = "system:user:add")
│   └── RequireLog                  ← 日志注解 (module, action, description)
├── context/                        ← 已存在
│   └── UserContext                 ← ThreadLocal 用户上下文 (userId, username)
└── util/                           ← 已存在
    ├── JwtUtil                     ← JWT 生成/验证/解析
    ├── PasswordEncoderUtil         ← BCrypt 加密/验证
    └── HttpContextUtil             ← HTTP 上下文工具 (获取IP等)
```

### 2.3 接口详细设计（Controller → Service → Mapper）

#### 2.3.1 AuthController — 认证

```
POST /api/auth/login
  输入: LoginDTO {username, password}
  处理: IAuthService.login()
  输出: R<LoginVO> {accessToken, refreshToken, expiresIn, userInfo}
  流程:
    1. 根据 username 查询 sys_user
    2. 验证用户状态 status == 1
    3. PasswordEncoderUtil.matches(明文, BCrypt密码)
    4. 更新 last_login_time
    5. JwtUtil.generateToken(userId, username)
    6. JwtUtil.generateRefreshToken(userId, username)
    7. 查询用户角色列表、部门名称
    8. 返回 LoginVO

POST /api/auth/logout
  输入: Authorization Header (Bearer token)
  处理: IAuthService.logout(token)
  输出: R<Void>
  流程:
    1. 解析 token 获取 userId
    2. 将 token 加入 Redis 黑名单 (TTL = token 剩余有效期)
    3. 记录审计日志 (@RequireLog)

POST /api/auth/refresh
  输入: refreshToken 参数
  处理: IAuthService.refreshToken(refreshToken)
  输出: R<LoginVO>
  流程:
    1. JwtUtil.validateToken(refreshToken)
    2. 检查 claims.type == "refresh"
    3. 检查 Redis 黑名单
    4. 生成新的 accessToken + refreshToken
    5. 将旧 refreshToken 加入黑名单

GET /api/auth/info
  输入: Authorization Header (从 JwtInterceptor 获取 userId)
  处理: 查询用户详情
  输出: R<UserInfo> {id, username, realName, deptName, phone, email}
```

**需创建的 Service 方法**:
```java
public interface IAuthService {
    LoginVO login(LoginDTO dto);
    void logout(String token);
    LoginVO refreshToken(String refreshToken);
}
```

**需创建的 Mapper 方法**:
```java
public interface SysUserMapper extends BaseMapper<SysUser> {
    SysUser selectByUsername(String username);
    @Update("UPDATE sys_user SET last_login_time = NOW() WHERE id = #{id}")
    int updateLastLoginTime(Long id);
}
```

#### 2.3.2 UserController — 用户管理

```
GET /api/system/user/page?pageNum=1&pageSize=10&username=&realName=&deptId=&status=
  处理: ISysUserService.pageUser()
  输出: R<PageResult<UserVO>>
  流程:
    1. LambdaQueryWrapper 构建条件
    2. 分页查询 sys_user
    3. 关联查询 dept_name, role_list
    4. 转换为 UserVO

GET /api/system/user/{id}
  处理: ISysUserService.getUserDetail(id)
  输出: R<UserVO>

POST /api/system/user
  输入: UserDTO {username, password, realName, deptId, phone, email, roleIds}
  处理: ISysUserService.addUser()
  输出: R<Void>
  流程:
    1. 检查 username 是否已存在
    2. PasswordEncoderUtil.encode(明文密码)
    3. 插入 sys_user
    4. 插入 sys_user_role 关联记录
    5. 记录审计日志

PUT /api/system/user
  处理: ISysUserService.updateUser()
  流程:
    1. 查询现有用户
    2. 更新字段（密码单独处理）
    3. 更新角色关联（先删除旧关联，再插入新关联）
    4. 记录审计日志

DELETE /api/system/user/{id}
  处理: ISysUserService.deleteUser()
  流程:
    1. 检查是否为超级管理员（不可删除）
    2. 删除 sys_user_role 关联
    3. 删除 sys_user 记录
    4. 记录审计日志

PUT /api/system/user/reset-password
  输入: ResetPasswordDTO {userId, newPassword}
  处理: ISysUserService.resetPassword()
  流程: PasswordEncoderUtil.encode → update

PUT /api/system/user/status/{id}?status=0/1
  处理: ISysUserService.changeStatus()

PUT /api/system/user/roles/{userId}
  输入: List<Long> roleIds
  处理: ISysUserService.assignRoles()
  流程: 删除旧关联 + 插入新关联
```

#### 2.3.3 RoleController — 角色管理

```
GET /api/system/role/page
  处理: ISysRoleService.pageRole()
  输出: R<PageResult<RoleVO>>

GET /api/system/role/{id}
  处理: 查询角色 + 获取关联菜单ID列表
  输出: R<RoleVO> {id, roleName, roleCode, description, status, menuIds}

POST /api/system/role
  输入: RoleDTO {roleName, roleCode, description, status, menuIds}
  处理: ISysRoleService.addRole()
  流程:
    1. 检查 roleCode 是否重复
    2. 插入 sys_role
    3. 批量插入 sys_role_menu
    4. 记录审计日志

PUT /api/system/role
  处理: ISysRoleService.updateRole()
  流程: 更新角色信息 + 重建菜单关联

DELETE /api/system/role/{id}
  处理: ISysRoleService.deleteRole()
  流程:
    1. 检查是否有用户关联此角色
    2. 删除 sys_role_menu 关联
    3. 删除 sys_role 记录

PUT /api/system/role/menus/{roleId}
  输入: List<Long> menuIds
  处理: ISysRoleService.assignMenus()

GET /api/system/role/{roleId}/users
  处理: ISysRoleService.getUsersByRoleId()
```

#### 2.3.4 MenuController — 菜单管理

```
GET /api/system/menu/tree
  处理: ISysMenuService.getMenuTree()
  输出: R<List<MenuTreeVO>>
  流程:
    1. 查询所有菜单（按 sort 排序）
    2. 递归构建树形结构（parentId = 0 为根）
    3. 每个节点包含 children 列表

GET /api/system/menu/user-tree
  处理: ISysMenuService.getMenuTreeByUserId(userId)
  流程:
    1. 通过 userId → sys_user_role → sys_role_menu → sys_menu
    2. 过滤 visible = 1 且 status = 1
    3. 构建树形结构

POST /api/system/menu
  输入: MenuDTO
  处理: ISysMenuService.addMenu()
  流程:
    1. 检查 parentId 是否存在（非0时）
    2. 插入 sys_menu
    3. 记录审计日志

PUT /api/system/menu
  处理: ISysMenuService.updateMenu()

DELETE /api/system/menu/{id}
  处理: ISysMenuService.deleteMenu()
  流程:
    1. 检查是否有子菜单
    2. 检查是否有角色关联此菜单
    3. 删除 sys_role_menu 关联
    4. 删除 sys_menu
```

#### 2.3.5 DeptController — 部门管理

```
GET /api/system/dept/tree
  处理: ISysDeptService.getDeptTree()
  输出: R<List<DeptTreeVO>>

POST /api/system/dept
  输入: DeptDTO

PUT /api/system/dept

DELETE /api/system/dept/{id}

GET /api/system/dept/{deptId}/users
```

#### 2.3.6 ClassificationController — 定密管理

```
GET /api/system/classification/list
  输出: 五级密级列表（绝密、机密、秘密、内部、公开）

GET /api/system/classification/accessible
  处理: ISysClassificationLevelService.getAccessibleLevels(userId)
  输出: R<ClassificationAccessVO> {maxLevel: 3, accessibleLevels: [3,4,5]}
  流程:
    1. 查询用户的角色
    2. 根据角色配置获取最高可访问密级
    3. 返回该密级及以下的所有密级

POST /api/system/classification
PUT /api/system/classification
DELETE /api/system/classification/{id}
```

#### 2.3.7 AuditLogController — 审计日志

```
GET /api/system/audit-log/page?pageNum=1&pageSize=10&module=&action=&userId=&startTime=&endTime=
  处理: ISysAuditLogService.pageLog()
  流程:
    1. LambdaQueryWrapper 构建时间范围 + 模块 + 操作类型条件
    2. 分页查询 sys_audit_log
    3. 关联查询 username（从 sys_user）
    4. 转换为 AuditLogVO
```

### 2.4 数据流图

#### 登录流程
```
前端 POST /api/auth/login {username, password}
    ↓
AuthController.login()
    ↓
IAuthService.login()
    ↓
SysUserMapper.selectByUsername(username)
    ↓ [查询 sys_user 表]
    ↓ 验证密码: PasswordEncoderUtil.matches(明文, BCrypt)
    ↓ 更新 last_login_time
    ↓ JwtUtil.generateToken(userId, username) → accessToken
    ↓ JwtUtil.generateRefreshToken(userId, username) → refreshToken
    ↓ 查询部门名称、角色列表
    ↓
返回 R<LoginVO> {accessToken, refreshToken, expiresIn, userInfo}
    ↓
前端存储 token 到 localStorage
    ↓ 后续请求携带 Authorization: Bearer {token}
    ↓
JwtInterceptor.preHandle() 拦截
    ↓ 验证 token → 设置 request.setAttribute("userId", userId)
    ↓ 放行到目标 Controller
```

#### 权限检查流程
```
请求到达 Controller 方法
    ↓
JwtInterceptor 验证 Token → 设置 userId 到 request
    ↓
方法有 @RequirePermission("system:user:add") 注解
    ↓
PermissionInterceptor (需创建) 拦截
    ↓
1. 从 request 获取 userId
2. userId → sys_user_role → 获取 roleIds
3. roleIds → sys_role_menu → 获取 menuIds
4. menuIds → sys_menu → 获取 perms 列表
5. 检查 "system:user:add" 是否在 perms 列表中
    ↓ 通过 → 执行目标方法
    ↓ 不通过 → 返回 403 禁止访问
    ↓
@RequireLog 切面记录操作日志 → sys_audit_log
```

### 2.5 错误处理

| 场景 | 异常类型 | HTTP | 错误码 | 消息 |
|------|----------|------|--------|------|
| 用户名不存在 | BusinessException | 200 | 401 | 用户名或密码错误 |
| 密码错误 | BusinessException | 200 | 401 | 用户名或密码错误 |
| 用户已禁用 | BusinessException | 200 | 403 | 用户已被禁用 |
| Token 无效 | (JwtInterceptor处理) | 401 | 401 | 令牌无效或已过期 |
| Token 在黑名单 | (JwtInterceptor处理) | 401 | 401 | 令牌已失效 |
| 无权限 | (PermissionInterceptor处理) | 403 | 403 | 没有权限执行此操作 |
| 用户名重复 | BusinessException | 200 | 400 | 用户名已存在 |
| 角色编码重复 | BusinessException | 200 | 400 | 角色编码已存在 |
| 超级管理员不可删 | BusinessException | 200 | 400 | 超级管理员不可删除 |
| 角色被用户使用 | BusinessException | 200 | 400 | 该角色下有用户，无法删除 |
| 菜单有子菜单 | BusinessException | 200 | 400 | 请先删除子菜单 |
| 菜单被角色引用 | BusinessException | 200 | 400 | 该菜单已被角色引用 |

### 2.6 数据库表

| 表 | 字段数 | 索引 | 说明 |
|---|--------|------|------|
| sys_dept | 9 | idx_parent_id, idx_status | 部门树 |
| sys_role | 7 | uk_role_code | 角色 |
| sys_user | 10 | uk_username, idx_dept_id, idx_status | 用户 |
| sys_user_role | 4 | uk_user_role, idx_role_id | 用户角色关联 |
| sys_menu | 12 | idx_parent_id, idx_type, idx_status | 菜单/权限 |
| sys_role_menu | 4 | uk_role_menu, idx_menu_id | 角色菜单关联 |
| sys_classification_level | 6 | uk_level_code | 密级字典 |
| sys_audit_log | 14 | idx_user_id, idx_module, idx_action, idx_result, idx_create_time | 审计日志 |

### 2.7 需创建的文件清单（auth 模块）

| 文件 | 类型 | 预估行数 | 优先级 |
|------|------|----------|--------|
| IAuthService.java | 接口 | 20 | P0 |
| AuthServiceImpl.java | 实现 | 120 | P0 |
| ISysUserService.java | 接口 | 40 | P0 |
| SysUserServiceImpl.java | 实现 | 200 | P0 |
| ISysRoleService.java | 接口 | 35 | P0 |
| SysRoleServiceImpl.java | 实现 | 180 | P0 |
| ISysMenuService.java | 接口 | 30 | P0 |
| SysMenuServiceImpl.java | 实现 | 150 | P0 |
| ISysDeptService.java | 接口 | 25 | P0 |
| SysDeptServiceImpl.java | 实现 | 120 | P0 |
| ISysClassificationLevelService.java | 接口 | 25 | P0 |
| SysClassificationLevelServiceImpl.java | 实现 | 80 | P0 |
| ISysAuditLogService.java | 接口 | 15 | P0 |
| SysAuditLogServiceImpl.java | 实现 | 50 | P0 |
| SysUserMapper.java | Mapper | 20 | P0 |
| SysRoleMapper.java | Mapper | 20 | P0 |
| SysMenuMapper.java | Mapper | 20 | P0 |
| SysDeptMapper.java | Mapper | 20 | P0 |
| SysUserRoleMapper.java | Mapper | 15 | P0 |
| SysRoleMenuMapper.java | Mapper | 15 | P0 |
| SysClassificationLevelMapper.java | Mapper | 15 | P0 |
| SysAuditLogMapper.java | Mapper | 20 | P0 |
| PermissionInterceptor.java | 拦截器 | 60 | P0 |
| **小计** | **23 个文件** | **~1,300 行** | |

---

## 三、document 模块详细设计

### 3.1 模块职责

智能文档解析：
- 支持 .doc/.docx/.pdf/.png/.jpg 等格式
- 解析文档结构（段落、表格、标题层级）
- 调用 LLM 进行段落分类（总则/分则/附则/法律责任等）
- 调用 LLM 生成向量（用于语义检索）
- 异步任务管理

### 3.2 完整类图

```
com.intelligent.trial.document
├── controller/
│   └── DocumentParseController       ← /api/document/parse/* (上传/任务/结果/重试)
├── service/
│   └── DocumentParseService          ← 核心解析服务（已有完整实现）
├── client/
│   └── LlmClient                     ← DashScope API 调用（段落分类/OCR/向量生成）
├── mapper/
│   └── DocParseTaskMapper            ← 解析任务 CRUD（已有，继承 BaseMapper）
├── entity/
│   └── DocParseTask                  ← {id, fileName, filePath, fileType, status, progress, resultJson, errorMsg, parseTime, vectorCount}
├── dto/
│   └── ParseResultDTO                ← {List<ParagraphDTO>, MetadataDTO}
├── vo/
│   └── ParseTaskVO                   ← 任务 VO（用于前端展示）
├── config/
│   ├── AsyncConfig                   ← 异步线程池配置 (core=4, max=8, queue=100)
│   ├── DashScopeConfig               ← DashScope API 配置
│   ├── MinioConfig                   ← MinIO 配置（旧版，保留兼容）
│   └── MinioClientConfig             ← MinIO 配置（新版，创建 MinioClient Bean）
└── util/
    ├── MinioUtil                     ← MinIO 工具（上传/下载/删除/URL）
    └── WordParseUtil                 ← Word/PDF 解析工具
```

### 3.3 接口详细设计

#### DocumentParseController

```
POST /api/document/parse/upload
  输入: MultipartFile file
  处理: DocumentParseService.uploadAndCreateTask()
  输出: R<Long> taskId
  流程:
    1. 校验文件类型（doc/docx/pdf/png/jpg/jpeg/bmp/gif/tiff）
    2. MinioUtil.uploadFile(file) → objectKey
    3. 创建 DocParseTask 记录（status=0 待处理）
    4. 异步调用 asyncParse(taskId, objectKey, fileType)
    5. 返回 taskId

GET /api/document/parse/tasks?page=1&size=10&status=&fileType=
  处理: DocumentParseService.listTasks()
  输出: R<Page<DocParseTask>>

GET /api/document/parse/task/{taskId}
  处理: DocumentParseService.getTaskById()
  输出: R<DocParseTask>

GET /api/document/parse/task/{taskId}/result
  处理: 查询任务 → 解析 resultJson → 返回 ParseResultDTO
  输出: R<ParseResultDTO>

POST /api/document/parse/task/{taskId}/retry
  处理: DocumentParseService.retryTask(taskId)
  流程: 重置状态 → 重新触发 asyncParse
  输出: R<Void>
```

#### DocumentParseService.asyncParse() 流程

```
asyncParse(taskId, objectKey, fileType)
    ↓
    1. 更新 status=1 处理中, progress=5
    ↓
    2. 根据 fileType 分发:
       - doc/docx → parseWord()
       - pdf → parsePdf()
       - 图片 → parseImage()
    ↓
    3. parseWord/parsePdf/parseImage
       - 下载文件到临时目录
       - 使用 POI/PDFBox 解析
       - 提取段落列表（content, style, position）
       - progress → 40-55
    ↓
    4. 段落分类 (progress 60-80)
       LlmClient.classifyParagraphs(paragraphs)
       调用 qwen-plus，返回 category + lawLevel
    ↓
    5. 向量生成 (progress 80-95)
       LlmClient.generateEmbeddings(texts)
       调用 text-embedding-v3，返回 float[] 向量
       TODO: 存入 Milvus（当前仅回填 vectorId）
    ↓
    6. 更新 status=2 完成, progress=100
       保存 resultJson, parseTime, vectorCount
```

### 3.4 数据流图

```
用户上传文件 (.docx)
    ↓
POST /api/document/parse/upload
    ↓
DocumentParseController.upload()
    ↓
DocumentParseService.uploadAndCreateTask()
    ↓
1. MinioUtil.uploadFile() → MinIO
2. 创建 doc_parse_task 记录 → MySQL
3. 异步线程 asyncParse()
    ↓
asyncParse():
    1. WordParseUtil.parseWord() → 提取段落
    2. LlmClient.classifyParagraphs() → DashScope qwen-plus → 段落分类
    3. LlmClient.generateEmbeddings() → DashScope text-embedding-v3 → 向量
    4. TODO: Milvus 入库
    5. 更新 task 状态为完成
    ↓
用户查询任务结果
    ↓
GET /api/document/parse/task/{id}/result
    ↓
返回 ParseResultDTO {paragraphs: [{content, style, category, lawLevel, vectorId}], metadata}
```

### 3.5 LlmClient 接口

```java
// 段落分类
List<ParagraphDTO> classifyParagraphs(List<ParagraphDTO> paragraphs)
  → POST /chat/completions (qwen-plus)
  → 返回 JSON: [{index, category, law_level}, ...]
  → 回填到 ParagraphDTO.category / lawLevel

// OCR 识别（图片 URL）
String recognizeImage(String imageUrl, String description)
  → POST /chat/completions (qwen-vl-max)
  → 图片 + 文本提示 → 返回 OCR 文本

// OCR 识别（Base64）
String recognizeImageBase64(String imageBase64, String mimeType, String description)
  → POST /chat/completions (qwen-vl-max)
  → data:{mimeType};base64,... + 文本提示 → 返回 OCR 文本

// 向量生成
List<float[]> generateEmbeddings(List<String> texts)
  → POST /embeddings (text-embedding-v3)
  → 返回 [{embedding: [float, ...]}, ...]
```

### 3.6 错误处理

| 场景 | 异常 | 处理 |
|------|------|------|
| 不支持的文件类型 | IllegalArgumentException | 返回 400，提示支持类型 |
| MinIO 上传失败 | RuntimeException | 任务状态=失败，记录 errorMsg |
| LLM API 超时 | IOException (120s) | 重试 1 次，仍失败则标记失败 |
| LLM 分类失败 | Exception (catch) | 段落保留但无分类信息，不中断流程 |
| 向量生成失败 | RuntimeException | 记录日志，vectorCount=0，任务仍标记完成 |
| 临时文件清理失败 | IOException (warn) | 记录警告，不阻塞流程 |

### 3.7 需补充

| 补充项 | 优先级 | 说明 |
|--------|--------|------|
| Milvus 向量入库 | P1 | 在 generateAndStoreVectors() 中接入 Milvus Java SDK |
| 解析任务关联到 repo_document | P1 | 解析完成后，创建 repo_document 记录 |
| 任务轮询 WebSocket/SSE | P2 | 前端实时接收进度更新 |
| 大文件分块解析 | P2 | 超过 100 页的 PDF 分批处理 |

---

## 四、repository 模块详细设计

### 4.1 模块职责

四库统一管理：
- 法规库（repo_type=1）、资料库（2）、裁判文书库（3）、案例库（4）
- 文档 CRUD + 多条件搜索 + 分页
- 目录树管理（最多 10 级嵌套）
- 文件上传/下载/预览（MinIO）
- Excel 批量导入/导出目录

### 4.2 完整类图

```
com.intelligent.trial.repository
├── controller/
│   ├── DocumentController            ← /api/repository/document/* (CRUD/搜索/上传/预览/下载)
│   └── DirectoryController           ← /api/repository/directory/* (CRUD/树/移动/排序/导入导出)
├── service/
│   ├── DocumentService               ← 文档服务接口
│   ├── DocumentServiceImpl           ← 文档服务实现（完整）
│   ├── DirectoryService              ← 目录服务接口
│   └── DirectoryServiceImpl          ← 目录服务实现（完整）
├── mapper/
│   ├── DocumentMapper                ← 文档 Mapper (searchDocuments)
│   └── DirectoryMapper               ← 目录 Mapper (selectByRepoTypeAndParentId/selectByPathPrefix/countChildren)
├── entity/
│   ├── Document                      ← 文档实体 {repoType, directoryId, title, docNo, publishUnit, publishDate, effectiveDate, revisionDate, validityStatus, classificationLevelId, filePath, fileSize, fileType, summary, vectorId, status}
│   └── Directory                     ← 目录实体 {repoType, parentId, name, sort, classificationLevelId, permissionScope, path, status}
├── dto/
│   └── DocumentSearchDTO             ← 搜索条件 {repoType, keyword, directoryId, validityStatus, publishDateStart/End, classificationLevelId, publishUnit} + PageRequest
├── vo/
│   └── DirectoryTreeVO               ← 目录树节点 {id, parentId, name, repoType, sort, classificationLevelId, children}
└── config/
    ├── MybatisPlusConfig             ← 分页插件 + 自动填充
    └── MinioConfig                   ← MinIO 配置
```

### 4.3 接口详细设计

#### DocumentController

```
POST /api/repository/document/create
  输入: Document {repoType, directoryId, title, docNo, publishUnit, ...}
  处理: DocumentService.create()
  流程:
    1. 默认 status=0 草稿
    2. 插入 repo_document
    3. 返回创建后的文档（含 ID）

PUT /api/repository/document/update
  输入: Document
  处理: DocumentService.update()
  流程:
    1. 检查文档是否存在
    2. 更新字段
    3. 返回更新后的文档

DELETE /api/repository/document/delete/{id}
  处理: DocumentService.delete()
  流程:
    1. 检查文档是否存在
    2. 删除 MinIO 文件（失败不阻止）
    3. 删除数据库记录

GET /api/repository/document/get/{id}
  处理: DocumentService.getById()

POST /api/repository/document/search
  输入: DocumentSearchDTO {repoType, keyword, directoryId, validityStatus, publishDateStart/End, classificationLevelId, publishUnit, pageNum, pageSize}
  处理: DocumentService.search()
  流程:
    1. 调用 DocumentMapper.searchDocuments()（自定义 SQL）
    2. SQL: WHERE repo_type=? AND (title LIKE ? OR doc_no LIKE ? OR publish_unit LIKE ?) AND ...
    3. ORDER BY publish_date DESC, create_time DESC
    4. 返回 IPage<Document>

POST /api/repository/document/upload
  输入: @RequestPart("document") Document, @RequestPart("file") MultipartFile
  处理: DocumentService.upload()
  流程:
    1. 校验文件非空
    2. 生成 MinIO objectName: documents/{timestamp}/{uuid}.{ext}
    3. minioClient.putObject() → MinIO
    4. 设置 document.filePath, fileSize, fileType
    5. 默认 status=1 已发布
    6. 插入 repo_document
    7. 返回创建后的文档

POST /api/repository/document/batch-upload?directoryId=&repoType=
  输入: List<MultipartFile> files
  处理: DocumentService.batchUpload()
  流程: 遍历文件 → 逐个 upload() → 返回列表

GET /api/repository/document/preview/{id}
  处理: DocumentService.preview()
  流程:
    1. 获取文档信息
    2. minioClient.getObject() → InputStream
    3. 流式写入 HttpServletResponse（不加载到内存）
    4. Content-Type 根据 fileType 设置

GET /api/repository/document/download/{id}
  处理: DocumentService.download()
  流程:
    1. minioClient.getObject() → InputStream
    2. 读取全部字节到内存（注意大文件问题）
    3. 设置 Content-Disposition: attachment; filename*=UTF-8''...
    4. 返回 ResponseEntity<byte[]>
```

#### DirectoryController

```
POST /api/repository/directory/create
  输入: Directory {repoType, parentId, name, sort, classificationLevelId, permissionScope}
  处理: DirectoryService.create()
  流程:
    1. 校验层级深度（≤10）
    2. 默认 sort=0, status=1, parentId=0
    3. 构建 path: buildPath(parentId) → "/0" → "/0/5" → "/0/5/12"
    4. 插入 repo_directory

PUT /api/repository/directory/update
  处理: DirectoryService.update()
  流程:
    1. 检查目录是否存在
    2. 如果 parentId 变化 → 校验层级 + 更新 path
    3. 更新字段

DELETE /api/repository/directory/delete/{id}
  处理: DirectoryService.delete()
  流程:
    1. 查询目录
    2. 查询所有子孙目录（path LIKE '/0/5/%'）
    3. 批量删除子目录
    4. 删除当前目录

GET /api/repository/directory/tree?repoType=1
  处理: DirectoryService.getTree()
  流程:
    1. 查询该库类型下所有目录（status=1）
    2. 按 sort + createTime 排序
    3. 转换为 DirectoryTreeVO
    4. 递归构建树形结构

PUT /api/repository/directory/move/{id}?newParentId=
  处理: DirectoryService.move()
  流程:
    1. 检查目录存在
    2. 检查不能移动到自己的子目录下（isDescendant）
    3. 校验移动后层级深度
    4. 更新当前目录的 parentId + path
    5. 更新所有子目录的 path（正则替换前缀）

PUT /api/repository/directory/sort/{id}?sort=
POST /api/repository/directory/import?repoType=
GET /api/repository/directory/export?repoType=
```

### 4.4 SQL 设计

#### DocumentMapper.searchDocuments

```sql
SELECT * FROM repo_document
WHERE 
  repo_type = ?                          -- 库类型过滤
  AND (title LIKE ? OR doc_no LIKE ? OR publish_unit LIKE ?)  -- 关键词模糊
  AND directory_id = ?                   -- 目录过滤
  AND validity_status = ?                -- 效力状态过滤
  AND publish_date >= ?                  -- 发布日期范围
  AND publish_date <= ?
  AND classification_level_id = ?        -- 密级过滤
  AND publish_unit LIKE ?                -- 发布单位模糊
ORDER BY publish_date DESC, create_time DESC
```

**注意**: 当前 SQL 没有权限过滤，需要加上定密级别过滤。

#### DirectoryMapper 查询

```sql
-- 按库类型和父ID查询
SELECT * FROM repo_directory
WHERE repo_type = ? AND parent_id = ?
ORDER BY sort ASC, create_time ASC

-- 按路径前缀查询（获取子树）
SELECT * FROM repo_directory
WHERE path LIKE ?
ORDER BY path ASC, sort ASC

-- 统计子目录数量
SELECT COUNT(1) FROM repo_directory
WHERE path LIKE CONCAT(
    (SELECT path FROM repo_directory WHERE id = ?), '%'
)
```

### 4.5 数据流图

```
用户上传文件 → POST /api/repository/document/upload
    ↓
DocumentService.upload()
    ↓
1. 生成 MinIO objectName
2. minioClient.putObject() → MinIO
3. 设置 Document.filePath/fileSize/fileType
4. 插入 repo_document → MySQL
    ↓
返回 Document {id, title, filePath, ...}

用户搜索文档 → POST /api/repository/document/search
    ↓
DocumentService.search()
    ↓
DocumentMapper.searchDocuments(Page, repoType, keyword, ...)
    ↓
执行 SQL → 返回 IPage<Document>
    ↓
返回给前端
```

### 4.6 错误处理

| 场景 | 异常 | 消息 |
|------|------|------|
| 文档不存在 | BusinessException | 文档不存在 |
| 上传文件为空 | BusinessException | 上传文件不能为空 |
| MinIO 上传失败 | BusinessException | 文件上传失败: {原因} |
| 预览文件不存在 | BusinessException | 文档文件不存在 |
| 目录不存在 | BusinessException | 目录不存在 |
| 目录层级超限 | BusinessException | 目录层级不能超过10级 |
| 移动到子目录 | BusinessException | 不能将目录移动到其子目录下 |

### 4.7 需补充

| 补充项 | 优先级 | 说明 |
|--------|--------|------|
| 定密级别权限过滤 | P0 | 搜索 SQL 中加上 classification_level 过滤 |
| 大文件下载优化 | P1 | 当前 download() 读取全部字节到内存，超过 50MB 会 OOM |
| 文档解析后自动创建 repo_document | P1 | document 模块解析完成后，调用 repository 创建文档记录 |
| 批量删除 | P2 | 支持多选删除 |

---

## 五、模块间依赖关系

### 5.1 合并后的依赖图

```
                    trial-common
                         ↑
        ┌────────────────┼────────────────┐
        ↓                ↓                ↓
    trial-auth      trial-document   trial-repository
        ↑                ↑                ↑
        │                │                │
        └────────────────┼────────────────┘
                         ↓
                    trial-application
              (统一启动入口，端口 8080)
```

### 5.2 模块间调用关系

| 调用方 | 被调用方 | 调用内容 | 调用方式 |
|--------|----------|----------|----------|
| document | common | R<T>, BusinessException, BaseEntity | Maven 依赖 |
| document | repository | 解析完成后创建 repo_document | 方法调用（合并后） |
| repository | common | R<T>, BusinessException, BaseEntity | Maven 依赖 |
| auth | common | R<T>, BusinessException, BaseEntity | Maven 依赖 |
| repository | auth | 查询用户密级进行权限过滤 | 方法调用（合并后） |
| document | auth | 获取当前用户信息 | UserContext.getUserId() |

### 5.3 合并策略

**方案**: 创建一个新的 `trial-application` 模块，作为统一启动入口。

```xml
<!-- trial-application/pom.xml -->
<dependencies>
    <dependency>
        <groupId>com.intelligent.trial</groupId>
        <artifactId>intelligent-trial-common</artifactId>
    </dependency>
    <dependency>
        <groupId>com.intelligent.trial</groupId>
        <artifactId>intelligent-trial-auth</artifactId>
    </dependency>
    <dependency>
        <groupId>com.intelligent.trial</groupId>
        <artifactId>intelligent-trial-document</artifactId>
    </dependency>
    <dependency>
        <groupId>com.intelligent.trial</groupId>
        <artifactId>intelligent-trial-repository</artifactId>
    </dependency>
</dependencies>
```

**Application.java**:
```java
@SpringBootApplication(scanBasePackages = "com.intelligent.trial")
@MapperScan("com.intelligent.trial.*.mapper")
public class TrialApplication {
    public static void main(String[] args) {
        SpringApplication.run(TrialApplication.class, args);
    }
}
```

---

## 六、前端详细设计

### 6.1 当前状态

```
frontend/src/
├── api/                    ← 7 个 API 文件（auth, user, role, menu, document, repository, audit-log）
├── views/
│   ├── Login.vue           ← ✅ 可用，但 login() 是 mock
│   ├── Layout.vue          ← ✅ 可用（简化版）
│   ├── Dashboard.vue       ← ✅ 可用，但静态数据
│   ├── Documents.vue       ← ❌ API 路径错误
│   ├── DocumentUpload.vue  ← ❌ API 路径错误
│   ├── Categories.vue      ← ❌ mock 数据
│   ├── document/Parse.vue  ← 存在但未路由
│   ├── repository/Index.vue  ← 存在但未路由
│   ├── repository/Directory.vue ← 存在但未路由
│   ├── system/User.vue     ← 存在但未路由
│   ├── system/Role.vue     ← 存在但未路由
│   ├── system/Menu.vue     ← 存在但未路由
│   └── system/Log.vue      ← 存在但未路由
├── router/index.js         ← 只有 6 个路由
├── store/                  ← user.js, tabs.js
├── utils/request.js        ← axios 封装（完整）
└── styles/global.scss      ← 全局样式
```

### 6.2 目标路由结构

```javascript
[
  { path: '/login', component: Login },                    // 登录
  {
    path: '/', component: Layout, redirect: '/dashboard',
    children: [
      // 工作台
      { path: 'dashboard', component: Dashboard },         // 工作台
      // 系统管理
      { path: 'system/user', component: SystemUser },      // 用户管理
      { path: 'system/role', component: SystemRole },      // 角色管理
      { path: 'system/menu', component: SystemMenu },      // 菜单管理
      { path: 'system/dept', component: SystemDept },      // 部门管理
      { path: 'system/log', component: SystemLog },        // 审计日志
      // 文档解析
      { path: 'document/upload', component: DocUpload },   // 上传解析
      { path: 'document/tasks', component: DocTasks },     // 任务列表
      { path: 'document/result/:id', component: DocResult }, // 解析结果
      // 多库管理
      { path: 'repo/:type', component: RepoIndex },        // 文档浏览
      { path: 'repo/:type/directory', component: RepoDir }, // 目录管理
      // 定密管理
      { path: 'classification', component: Classification }, // 定密管理
    ]
  }
]
```

### 6.3 需修复/创建的前端页面

| 页面 | 状态 | 需对接的 API | 优先级 |
|------|------|-------------|--------|
| Login.vue | mock | POST /api/auth/login | P0 |
| System/User.vue | 存在未路由 | GET/POST/PUT/DELETE /api/system/user/* | P0 |
| System/Role.vue | 存在未路由 | GET/POST/PUT/DELETE /api/system/role/* | P0 |
| System/Menu.vue | 存在未路由 | GET/POST/PUT/DELETE /api/system/menu/* | P1 |
| System/Log.vue | 存在未路由 | GET /api/system/audit-log/* | P1 |
| Document/Parse.vue | 存在未路由 | GET /api/document/parse/tasks, GET /api/document/parse/task/{id}/result | P0 |
| Repository/Index.vue | 存在未路由 | POST /api/repository/document/search | P0 |
| Repository/Directory.vue | 存在未路由 | GET/POST/PUT/DELETE /api/repository/directory/* | P1 |

---

## 七、整体开发顺序

### 7.1 优先级矩阵

```
紧急 ↑
  │
  │  P0（今天-明天）
  │  · 初始化数据库
  │  · 创建 auth 模块所有 Service + Mapper（15个文件）
  │  · 实现 IAuthService（登录）
  │  · 注册 JwtInterceptor + PermissionInterceptor
  │  · 创建 trial-application 统一启动入口
  │
  │  P1（本周）
  │  · 前端 Login 对接真实 API
  │  · 前端路由重构（所有页面接入）
  │  · 系统管理页面对接（用户/角色/菜单/日志）
  │  · 文档解析页面修复
  │  · 多库管理页面对接
  │  · 定密级别权限过滤
  │
  │  P2（第 2 周）
  │  · Milvus 向量入库
  │  · 文档解析结果关联到 repo_document
  │  · 大文件下载优化
  │  · BaseEntity 自动填充
  │
  │  P3（第 3-4 周）
  │  · 类案智能推送
  │  · 文书辅助生成
  │  · Flowable 工作流
  │  · 处分执行
  └──────────────────────────────────→ 重要性
```

### 7.2 详细开发任务分解

#### 阶段一：认证链路打通（2-3 天）

| # | 任务 | 产出 | 预估工时 |
|---|------|------|----------|
| 1.1 | 执行 init.sql 初始化数据库 | 17 张表 + 初始数据 | 0.5h |
| 1.2 | 修复后端启动（数据库连接） | 8080 端口正常启动 | 1h |
| 1.3 | 创建 auth 模块 8 个 Mapper 接口 | 8 个 Java 文件 | 2h |
| 1.4 | 创建 auth 模块 7 个 Service 接口 | 7 个 Java 文件 | 1h |
| 1.5 | 实现 AuthServiceImpl（登录/登出/刷新） | 120 行代码 | 3h |
| 1.6 | 实现 SysUserServiceImpl（用户 CRUD） | 200 行代码 | 4h |
| 1.7 | 实现 SysRoleServiceImpl（角色 CRUD） | 180 行代码 | 3h |
| 1.8 | 实现 SysMenuServiceImpl（菜单树） | 150 行代码 | 3h |
| 1.9 | 实现 SysDeptServiceImpl（部门树） | 120 行代码 | 2h |
| 1.10 | 实现 ClassificationLevelServiceImpl | 80 行代码 | 1.5h |
| 1.11 | 实现 AuditLogServiceImpl | 50 行代码 | 1h |
| 1.12 | 创建 PermissionInterceptor | 60 行代码 | 2h |
| 1.13 | 创建 trial-application 统一启动 | 1 个 pom + 1 个 Application.java | 1h |
| 1.14 | 前端 Login 对接真实 API | 修改 Login.vue | 1h |
| 1.15 | 前端路由重构 | 修改 router/index.js | 1h |

#### 阶段二：核心功能对接（3-5 天）

| # | 任务 | 产出 | 预估工时 |
|---|------|------|----------|
| 2.1 | 系统管理页面对接（用户/角色/菜单/日志） | 4 个页面 | 8h |
| 2.2 | 文档解析页面修复 | Upload + Task + Result | 6h |
| 2.3 | 多库管理页面对接 | Repository Index + Directory | 8h |
| 2.4 | 定密级别权限过滤 | SQL 修改 + 拦截器 | 3h |
| 2.5 | 完整流程测试：登录→上传→解析→查看 | 端到端测试 | 2h |

---

> **文档版本历史**
> 
> | 版本 | 日期 | 变更 |
> |------|------|------|
> | v1.0 | 2026-05-14 | 逐模块详细设计，包含完整类图、接口定义、数据流、错误处理 |

