<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.intelligent.trial.document.mapper.CaseSimilarityRecordMapper">

    <!-- 通用结果映射 -->
    <resultMap id="BaseResultMap" type="com.intelligent.trial.document.entity.CaseSimilarityRecord">
        <id column="id" property="id"/>
        <result column="source_case_id" property="sourceCaseId"/>
        <result column="similar_case_id" property="similarCaseId"/>
        <result column="similarity_score" property="similarityScore"/>
        <result column="content_score" property="contentScore"/>
        <result column="amount_score" property="amountScore"/>
        <result column="type_score" property="typeScore"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 插入或更新（ON DUPLICATE KEY UPDATE） -->
    <insert id="insertOrUpdate" parameterType="com.intelligent.trial.document.entity.CaseSimilarityRecord"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO case_similarity_record
            (source_case_id, similar_case_id, similarity_score, content_score, amount_score, type_score)
        VALUES
            (#{sourceCaseId}, #{similarCaseId}, #{similarityScore}, #{contentScore}, #{amountScore}, #{typeScore})
        ON DUPLICATE KEY UPDATE
            similarity_score = VALUES(similarity_score),
            content_score = VALUES(content_score),
            amount_score = VALUES(amount_score),
            type_score = VALUES(type_score),
            update_time = NOW()
    </insert>

    <!-- 批量插入或更新 -->
    <insert id="batchInsertOrUpdate" parameterType="java.util.List">
        INSERT INTO case_similarity_record
            (source_case_id, similar_case_id, similarity_score, content_score, amount_score, type_score)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.sourceCaseId}, #{item.similarCaseId}, #{item.similarityScore},
             #{item.contentScore}, #{item.amountScore}, #{item.typeScore})
        </foreach>
        ON DUPLICATE KEY UPDATE
            similarity_score = VALUES(similarity_score),
            content_score = VALUES(content_score),
            amount_score = VALUES(amount_score),
            type_score = VALUES(type_score),
            update_time = NOW()
    </insert>

</mapper>
