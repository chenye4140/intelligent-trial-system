# 智能审理系统 — 架构设计与开发规划

> **版本**: v2.0（架构设计版）  
> **编制日期**: 2026-05-14  
> **编制**: 小墨 (Xiao Mo)  
> **状态**: 架构评审中

---

## 零、产品本质思考

### 0.1 这个产品到底解决什么问题？

纪检监察审理系统的**核心痛点**：

```
传统方式:                     我们的方案:
┌─────────────────────┐       ┌─────────────────────┐
│ 1. 纸质卷宗翻阅       │  →    │ 1. 一键上传，AI 解析 │
│ 2. 凭经验找类案       │  →    │ 2. 智能推送相似案件  │
│ 3. 手工写审理报告     │  →    │ 3. 模板 + AI 辅助生成│
│ 4. Excel 跟踪处分     │  →    │ 4. 全流程数字化管理  │
│ 5. 权限靠人工管理     │  →    │ 5. 五级定密自动控制  │
└─────────────────────┘       └─────────────────────┘
```

**一句话价值主张**:
> 让审理人员从"翻材料、找案例、写报告"的重复劳动中解放出来，把精力集中在"判断和决策"上。

### 0.2 用户画像与核心场景

| 角色 | 典型场景 | 核心需求 | 使用频率 |
|------|----------|----------|----------|
| **审理人员** | 上传案件材料 → 查看 AI 解析结果 → 参考类案 → 生成审理报告 | 文档解析质量、类案准确度、文书模板 | 每天 |
| **审批领导** | 查看待审批案件 → 审核审理报告 → 批准/退回 | 审批流程清晰、关键信息一目了然 | 每周 |
| **系统管理员** | 管理用户/角色/权限 → 维护知识库 → 查看审计日志 | 权限管理灵活、系统稳定 | 偶尔 |
| **执行跟踪人员** | 跟踪处分执行情况 → 提醒到期事项 → 归档 | 执行状态清晰、自动提醒 | 每周 |

### 0.3 用户旅程（核心流程）

```
┌──────────────────────────────────────────────────────────────────────┐
│                        用户核心旅程                                    │
│                                                                      │
│  [登录] → [工作台] → [上传案件材料] → [AI 自动解析]                   │
│     ↓                                                                  │
│  [查看解析结果] → [查看 AI 推送的类案] → [参考类案量刑]               │
│     ↓                                                                  │
│  [生成审理报告] → [提交审批] → [领导审批] → [作出处分决定]            │
│     ↓                                                                  │
│  [处分执行跟踪] → [到期提醒] → [执行完毕归档] → [以案促改]             │
└──────────────────────────────────────────────────────────────────────┘
```

---

## 一、当前架构深度剖析

### 1.1 现有模块地图

```
intelligent-trial-system/
├── common/          ✅ 完整   公共工具：BaseEntity, R<T>, 异常处理
├── auth/            ⚠️ 半成品  认证授权：Controller 有，Service 未实现，拦截器有但未启用
├── document/        ✅ 较完整  文档解析：上传 → 解析 → AI 分类 → 向量生成（完整链路）
├── repository/      ✅ 较完整  多库管理：CRUD + 搜索 + 上传 + 下载（完整 Service）
├── workflow/        ❌ 空壳    只有 Application.java 和 application.yml
├── api-gateway/     ❌ 空壳    只有 Application.java 和 application.yml
└── frontend/        ⚠️ 半成品  12 个页面，路由混乱，API 路径错误
```

### 1.2 架构问题诊断

#### 🔴 致命问题

| 问题 | 严重度 | 说明 |
|------|--------|------|
| **伪微服务架构** | 高 | 6 个模块号称微服务，但无注册中心、无网关、无服务间通信，且都连同一数据库 |
| **认证链路断裂** | 高 | auth 模块有 JWT 拦截器和 SecurityConfig，但 IAuthService 未实现，登录功能完全不工作 |
| **前端路由混乱** | 中 | 有两套 Layout（简化和 Element Plus 版），12 个页面中 10 个未被主路由引用 |
| **API 路径不统一** | 中 | document 模块用 `/api/document/parse/...`，repository 用 `/api/repository/...`，前端调用的是第三套路径 |

#### 🟡 设计问题

| 问题 | 说明 |
|------|------|
| **模块职责重叠** | document 和 repository 都有文档管理功能，边界不清 |
| **无统一 API 网关** | 前端直接调用多个后端端口（8081/8082/8083），部署复杂 |
| **缺少权限过滤** | repository 的搜索接口没有按用户权限过滤数据 |
| **无服务间通信机制** | auth 模块无法被其他模块调用（无 Feign 客户端、无共享依赖） |

### 1.3 技术栈评估

| 技术 | 当前使用 | 评估 | 建议 |
|------|----------|------|------|
| Spring Boot 2.7.18 | ✅ 使用 | 接近 EOL（2024-11），建议升级 3.x | 第一阶段保持 2.7，第二阶段升级 |
| Java 8 | ✅ 使用 | 长期支持版本，但 StringBuilder 等 API 受限 | 第一阶段保持，第二阶段升 17 |
| MyBatis-Plus 3.5.5 | ✅ 使用 | 成熟稳定 | 保持 |
| Vue 3 + Element Plus | ✅ 使用 | 主流选择 | 保持 |
| MySQL 8.0 | ✅ 使用 | 主流选择 | 保持 |
| Redis 7.x | ✅ 使用 | 主流选择 | 保持 |
| MinIO | ✅ 使用 | 主流选择 | 保持 |
| Milvus 2.4 | ❌ 未启用 | 向量数据库，类案推送核心依赖 | 第二阶段启用 |
| Flowable | ❌ 未集成 | 工作流引擎，处分执行核心依赖 | 第三阶段集成 |

---

## 二、目标架构设计

### 2.1 架构决策：单体模块化 vs 微服务

**结论：第一阶段采用"单体模块化"架构**

| 维度 | 微服务 | 单体模块化 | 选择理由 |
|------|--------|------------|----------|
| 团队规模 | 适合 5+ 人团队 | 适合 1-3 人 | 当前开发人数少 |
| 部署复杂度 | 高（6 个服务 + 网关 + 注册中心） | 低（1 个 jar 包） | 快速迭代优先 |
| 运维成本 | 高 | 低 | 减少基础设施负担 |
| 开发效率 | 低（服务间调用、分布式事务） | 高（方法调用） | 功能开发优先 |
| 扩展性 | 高（可独立扩展） | 中 | 第二阶段可拆分 |

**单体模块化架构优势**:
- 每个模块仍然是独立的 Maven Module，代码组织清晰
- 通过 Maven 依赖实现模块间调用，无需 HTTP/RPC
- 未来需要时，可以将模块独立为微服务（已有清晰边界）

### 2.2 目标架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        前端层 (Vue 3)                        │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │ 登录/首页 │ │ 案件管理  │ │ 文档解析  │ │ 系统管理  │       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼ HTTP/JSON
┌─────────────────────────────────────────────────────────────┐
│                  统一后端服务 (Spring Boot)                   │
│  端口: 8080                                                  │
│  ┌─────────────────────────────────────────────────────┐    │
│  │                   API 路由层                          │    │
│  │  /api/auth/*     /api/case/*    /api/document/*     │    │
│  └─────────────────────────────────────────────────────┘    │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │ auth     │ │ case     │ │ document │ │ system   │       │
│  │ 模块     │ │ 模块     │ │ 模块     │ │ 模块     │       │
│  │ 认证授权  │ │ 案件管理  │ │ 文档解析  │ │ 系统管理  │       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐                    │
│  │ repo     │ │ workflow │ │ report   │                    │
│  │ 模块     │ │ 模块     │ │ 模块     │                    │
│  │ 多库管理  │ │ 工作流   │ │ 文书生成  │                    │
│  └──────────┘ └──────────┘ └──────────┘                    │
└─────────────────────────────────────────────────────────────┘
                              │
          ┌───────────────────┼───────────────────┐
          ▼                   ▼                   ▼
      ┌────────┐         ┌────────┐         ┌────────┐
      │ MySQL  │         │ Redis  │         │ MinIO  │
      │ 8.0    │         │ 7.x    │         │        │
      └────────┘         └────────┘         └────────┘
          │
          ▼
      ┌────────┐
      │ Milvus │  (第二阶段启用)
      │ 2.4    │
      └────────┘
```

### 2.3 模块重新规划

| 模块 | 职责 | 来源 | 说明 |
|------|------|------|------|
| **trial-common** | 公共工具类、DTO、异常处理 | common 改造 | 所有模块的基础依赖 |
| **trial-auth** | 用户认证、JWT、权限拦截、RBAC | auth 改造 | 实现 IAuthService，注册拦截器 |
| **trial-case** | 案件管理、案件信息 CRUD | 新建 | 案件基本信息、当事人、违纪事实 |
| **trial-document** | 文档解析、AI 分类、向量生成 | document 改造 | 保持现有逻辑，修复数据库连接 |
| **trial-repository** | 法规库/案例库/文书库管理 | repository 改造 | 四库统一管理，添加权限过滤 |
| **trial-workflow** | 审批流程、处分执行 | workflow 改造 | 集成 Flowable |
| **trial-report** | 文书模板、AI 辅助生成 | 新建 | 模板管理 + LLM 生成 |
| **trial-system** | 系统管理、审计日志、定密 | auth 拆分 | 用户/角色/菜单/日志管理 |

**模块依赖关系**:
```
trial-common (基础)
    ↑
    ├── trial-auth (依赖 common)
    ├── trial-case (依赖 common + auth)
    ├── trial-document (依赖 common + auth)
    ├── trial-repository (依赖 common + auth)
    ├── trial-workflow (依赖 common + auth + case)
    ├── trial-report (依赖 common + auth + document + case)
    └── trial-system (依赖 common + auth)
```

**统一入口**: 所有模块打包到一个 Spring Boot 应用（trial-application），端口 8080。

### 2.4 API 路由规划

```
/api
├── /auth              # 认证相关
│   ├── POST /login              登录
│   ├── POST /logout             退出
│   ├── GET  /info               获取用户信息
│   └── POST /refresh            刷新 Token
├── /system            # 系统管理
│   ├── /users           用户 CRUD
│   ├── /roles           角色 CRUD
│   ├── /menus           菜单 CRUD
│   ├── /depts           部门 CRUD
│   └── /audit-logs      审计日志查询
├── /case              # 案件管理
│   ├── /list              案件列表
│   ├── /create            创建案件
│   ├── /{id}              案件详情
│   ├── /{id}/materials    案件材料
│   └── /{id}/notes        阅卷笔记
├── /document          # 文档解析
│   ├── /parse/upload      上传解析
│   ├── /parse/tasks       任务列表
│   ├── /parse/task/{id}   任务详情
│   └── /parse/task/{id}/result  解析结果
├── /repository        # 多库管理
│   ├── /documents/search    搜索文档
│   ├── /documents/{id}      文档详情
│   ├── /documents/{id}/preview  预览
│   ├── /documents/{id}/download 下载
│   ├── /directories/tree    目录树
│   └── /similar             类案推送 (第二阶段)
├── /workflow          # 工作流 (第三阶段)
│   ├── /process/start       启动流程
│   ├── /tasks/my            我的待办
│   ├── /tasks/{id}/approve  审批
│   └── /punishment/*        处分执行
└── /report            # 文书生成 (第二阶段)
    ├── /templates           模板管理
    ├── /generate            生成文书
    └── /{id}/review         文书审核
```

---

## 三、前端架构设计

### 3.1 当前问题

```
现有前端结构 (混乱):
├── views/
│   ├── Login.vue              ← 路由引用，mock 实现
│   ├── Layout.vue             ← 路由引用，简化版
│   ├── Dashboard.vue          ← 路由引用，静态数据
│   ├── Documents.vue          ← 路由引用，API 路径错误
│   ├── DocumentUpload.vue     ← 路由引用，API 路径错误
│   ├── Categories.vue         ← 路由引用，mock 数据
│   ├── document/Parse.vue     ← 未被路由引用
│   ├── repository/            ← 未被路由引用
│   │   ├── Index.vue
│   │   └── Directory.vue
│   └── system/                ← 未被路由引用
│       ├── User.vue
│       ├── Role.vue
│       ├── Menu.vue
│       └── Log.vue
```

### 3.2 目标前端结构

```
frontend/src/
├── api/                    # API 请求层
│   ├── auth.js             # 认证 API
│   ├── case.js             # 案件 API
│   ├── document.js         # 文档 API
│   ├── repository.js       # 多库 API
│   ├── system.js           # 系统管理 API
│   └── request.js          # axios 封装
├── views/                  # 页面组件
│   ├── Login.vue           # 登录页
│   ├── Layout.vue          # 布局容器
│   ├── dashboard/          # 工作台
│   │   └── Index.vue
│   ├── case/               # 案件管理
│   │   ├── List.vue        # 案件列表
│   │   ├── Detail.vue      # 案件详情
│   │   └── Create.vue      # 创建案件
│   ├── document/           # 文档解析
│   │   ├── Upload.vue      # 上传解析
│   │   ├── Parse.vue       # 解析结果
│   │   └── TaskList.vue    # 任务列表
│   ├── repository/         # 多库管理
│   │   ├── Index.vue       # 文档浏览
│   │   ├── Search.vue      # 搜索
│   │   ├── Directory.vue   # 目录管理
│   │   └── Similar.vue     # 类案推送 (第二阶段)
│   ├── report/             # 文书生成 (第二阶段)
│   │   ├── Template.vue    # 模板管理
│   │   └── Generate.vue    # 文书生成
│   ├── workflow/           # 工作流 (第三阶段)
│   │   ├── MyTasks.vue     # 我的待办
│   │   └── Process.vue     # 流程详情
│   └── system/             # 系统管理
│       ├── User.vue        # 用户管理
│       ├── Role.vue        # 角色管理
│       ├── Menu.vue        # 菜单管理
│       └── AuditLog.vue    # 审计日志
├── components/             # 公共组件
│   ├── DocumentPreview.vue # 文档预览
│   ├── RichEditor.vue      # 富文本编辑器
│   └── TreeSelect.vue      # 树形选择器
├── router/                 # 路由配置
│   └── index.js
├── store/                  # 状态管理
│   ├── user.js             # 用户状态
│   ├── tabs.js             # 标签页状态
│   └── permission.js       # 权限状态
└── utils/                  # 工具函数
    ├── request.js          # axios 封装
    ├── auth.js             # 认证工具
    └── format.js           # 格式化工具
```

### 3.3 路由设计

```javascript
// 路由结构（按功能模块组织）
const routes = [
  { path: '/login', component: Login },
  {
    path: '/',
    component: Layout,
    redirect: '/dashboard',
    children: [
      // 工作台
      { path: 'dashboard', component: () => import('@/views/dashboard/Index.vue') },
      
      // 案件管理
      { path: 'case', component: () => import('@/views/case/List.vue') },
      { path: 'case/create', component: () => import('@/views/case/Create.vue') },
      { path: 'case/:id', component: () => import('@/views/case/Detail.vue') },
      
      // 文档解析
      { path: 'document/upload', component: () => import('@/views/document/Upload.vue') },
      { path: 'document/parse/:taskId', component: () => import('@/views/document/Parse.vue') },
      
      // 多库管理
      { path: 'repository', component: () => import('@/views/repository/Index.vue') },
      { path: 'repository/directory', component: () => import('@/views/repository/Directory.vue') },
      
      // 系统管理
      { path: 'system/user', component: () => import('@/views/system/User.vue') },
      { path: 'system/role', component: () => import('@/views/system/Role.vue') },
      { path: 'system/menu', component: () => import('@/views/system/Menu.vue') },
      { path: 'system/audit-log', component: () => import('@/views/system/AuditLog.vue') },
    ]
  }
]
```

---

## 四、数据库设计评估

### 4.1 现有表结构

| 表名 | 用途 | 状态 | 评价 |
|------|------|------|------|
| sys_dept | 部门 | ✅ 完整 | 树形结构，支持无限级 |
| sys_role | 角色 | ✅ 完整 | 基础字段完整 |
| sys_user | 用户 | ✅ 完整 | 包含部门、状态 |
| sys_user_role | 用户角色关联 | ✅ 完整 | 多对多 |
| sys_menu | 菜单 | ✅ 完整 | 树形，支持菜单/按钮 |
| sys_role_menu | 角色菜单关联 | ✅ 完整 | 多对多 |
| sys_classification_level | 定密级别 | ✅ 完整 | 五级定密 |
| sys_audit_log | 审计日志 | ✅ 完整 | 操作审计 |
| repo_directory | 文档目录 | ✅ 完整 | 树形，支持多库 |
| repo_document | 文档 | ✅ 完整 | 包含版本、状态、定密 |
| doc_parse_task | 解析任务 | ✅ 完整 | 任务状态跟踪 |
| case_similarity_record | 类案记录 | ✅ 完整 | 类案推送用 |
| punishment_execution | 处分执行 | ✅ 完整 | 执行状态跟踪 |
| punishment_material | 处分材料 | ✅ 完整 | 执行材料 |
| reading_note | 阅卷笔记 | ✅ 完整 | 笔记功能 |
| case_promotion | 以案促改 | ✅ 完整 | 促改管理 |
| incoming_doc | 来文登记 | ✅ 完整 | 智能办公 |

### 4.2 评估结论

**数据库设计质量: 🟢 良好 (80/100)**

| 维度 | 评分 | 说明 |
|------|------|------|
| 表结构完整性 | 85/100 | 17 张表覆盖了所有核心业务 |
| 索引设计 | 60/100 | 缺少复合索引，大数据量会慢 |
| 外键约束 | 50/100 | 未使用外键（合理，但需应用层保证） |
| 字段设计 | 85/100 | 字段命名规范，注释完整 |
| 扩展性 | 75/100 | 预留了版本管理、软删除 |

**需要改进**:
1. 添加复合索引：`(repo_type, status, create_time)` 等
2. 大字段（content）考虑拆分到单独表
3. 审计日志表考虑分表（按月）

---

## 五、开发策略

### 5.1 核心原则

```
┌────────────────────────────────────────────────────────┐
│                    开发原则                              │
│                                                        │
│  1. 先跑通，再优化                                        │
│     先让核心流程跑起来，再考虑性能和扩展性                  │
│                                                        │
│  2. 功能优先于架构                                       │
│     第一阶段不拆分微服务，先在一个应用中实现所有功能        │
│                                                        │
│  3. 用户价值优先                                        │
│     优先开发用户每天使用的功能（上传、解析、查看）          │
│     次要开发管理功能（用户管理、角色管理）                  │
│                                                        │
│  4. 可演进架构                                          │
│     代码组织按模块，未来可独立为微服务                     │
│                                                        │
│  5. 质量底线                                            │
│     认证必须工作、权限必须生效、数据必须安全               │
└────────────────────────────────────────────────────────┘
```

### 5.2 开发阶段规划

```
第 1 周: 打通核心流程
├── 修复后端启动（数据库连接）
├── 实现 IAuthService（登录功能）
├── 注册 JWT 拦截器
├── 前端 Login 对接
├── 统一 API 路由
└── 文档上传真实测试

第 2 周: 案件管理 + 多库对接
├── 案件管理 CRUD
├── 多库管理页面对接
├── 文档解析结果展示
├── 目录管理页面对接
└── 权限系统基础

第 3-4 周: AI 能力
├── Milvus 向量入库
├── 类案智能推送
├── 文书模板管理
├── AI 辅助生成
└── 前端页面完善

第 5-6 周: 工作流 + 权限
├── Flowable 集成
├── 审批流程
├── 处分执行
├── 五级定密控制
└── 审计日志

第 7-8 周: 生产就绪
├── 测试 + 修复
├── 性能优化
├── 文档完善
└── 安全加固
```

### 5.3 开发优先级矩阵

```
重要性 ↑
  │
  │  P0 (立即)          P1 (本周)
  │  · 数据库初始化       · 案件管理 CRUD
  │  · 用户登录           · 文档解析展示
  │  · JWT 拦截器         · 多库页面对接
  │  · 权限基础           · 目录管理
  │
  │  P2 (第 2 周)        P3 (后续)
  │  · AI 类案推送        · 工作流审批
  │  · 文书生成           · 处分执行
  │  · 阅卷笔记           · 以案促改
  │  · 来文登记           · 数据统计
  │
  └────────────────────────────────────→ 实现难度
```

### 5.4 不做什么（Out of Scope）

| 内容 | 原因 | 替代方案 |
|------|------|----------|
| 微服务拆分 | 团队小、基础设施不足 | 单体模块化，未来可拆分 |
| 前端 SSR | 内部系统，SEO 不重要 | SPA 足够 |
| 移动端 APP | 第一阶段不需要 | 响应式设计 |
| 实时推送 | 不需要 | 轮询足够 |
| 大数据分析 | 数据量不够 | 基础统计足够 |

---

## 六、技术选型总结

| 层级 | 技术 | 版本 | 用途 |
|------|------|------|------|
| **前端** | Vue 3 | 3.4 | 框架 |
| | Element Plus | 2.6 | UI 组件库 |
| | Axios | 1.6 | HTTP 客户端 |
| | Vue Router | 4.3 | 路由 |
| | Pinia | 2.1 | 状态管理 |
| | Vite | 5.2 | 构建工具 |
| **后端** | Spring Boot | 2.7.18 | 框架（第一阶段） |
| | MyBatis-Plus | 3.5.5 | ORM |
| | JWT (jjwt) | 0.12.5 | 认证 |
| | Flowable | 6.8.0 | 工作流（第三阶段） |
| | Apache POI | 5.2.5 | Word 解析 |
| | Apache PDFBox | 2.0.30 | PDF 解析 |
| **AI** | DashScope | - | LLM API |
| | qwen-plus | - | 段落分类 |
| | qwen-vl-max | - | OCR 识别 |
| | text-embedding-v3 | - | 向量生成 |
| **基础设施** | MySQL | 8.0 | 关系数据库 |
| | Redis | 7.x | 缓存/Token |
| | MinIO | Latest | 文件存储 |
| | Milvus | 2.4 | 向量数据库（第二阶段） |
| | Docker | Latest | 容器化 |

---

## 七、风险与应对

| 风险 | 概率 | 影响 | 应对 |
|------|------|------|------|
| DashScope API 限流 | 中 | 高 | 重试 + 降级 + 缓存 |
| 大模型分类准确率不足 | 中 | 中 | 人工修正入口 + prompt 优化 |
| PDF 扫描件解析质量 | 高 | 中 | OCR 回退方案 |
| 前端页面工作量大 | 高 | 中 | 优先核心页面，管理页面简化 |
| 数据库性能 | 低 | 低 | 第一阶段数据量小，后续优化 |

---

## 八、下一步行动

### 8.1 架构决策确认

在开始编码之前，需要确认以下架构决策：

1. **是否采用单体模块化架构？**（推荐是）
2. **是否需要保留现有的模块拆分？**（推荐是，但合并启动端口）
3. **前端是否需要完全重写？**（推荐保留可用页面，修复路由）
4. **第一阶段是否需要 Milvus？**（推荐否，第二阶段再启用）

### 8.2 立即执行项

一旦架构确认，按以下顺序执行：

```
Step 1: 修复基础设施
  └── 初始化数据库（执行 init.sql）
  └── 修复后端配置（统一端口 8080）

Step 2: 打通认证链路
  └── 实现 IAuthService
  └── 注册 JWT 拦截器
  └── 前端 Login 对接

Step 3: 统一 API 路由
  └── 合并 auth/document/repository 到一个应用
  └── 统一 API 前缀为 /api
  └── 修复前端 API 调用

Step 4: 核心功能验证
  └── 用户能登录
  └── 能上传文档并查看解析结果
  └── 多库管理页面显示真实数据
```

---

> **文档版本历史**
> 
> | 版本 | 日期 | 变更 |
> |------|------|------|
> | v1.0 | 2026-05-14 | 初始产品评估文档（528 行） |
> | v2.0 | 2026-05-14 | 架构设计与开发规划（本文档） |
